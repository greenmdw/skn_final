# 유아용품 가상제품 사용설명서 생성 설계

작성일: 2026-09-11 · 대상: TrueFit 데모·RAG 평가용 합성 데이터 · 상태: 구현 전 설계

## 1. 제안하는 방법

**가상제품의 구조화 데이터를 사실의 기준으로 삼고, 별도로 정의한 사용 절차·관리·경고 프로필을 결합하여 설명서를 생성한다. 수치·조건·금지는 규칙 기반 템플릿으로 출력하고, LLM은 허용된 문장의 표현을 다듬는 데만 사용한다.** 완성된 설명서를 다시 검사한 뒤 검색용 코퍼스로 게시한다.

생성 흐름은 다음과 같다.

```text
제품 카탈로그 + 가상 참조 데이터 + 버전이 고정된 사용 프로필
    → 입력·관계 검증
    → 사실 원장과 조건식 생성
    → 설명서 중간표현 구성
    → 필수 문장·표 생성 + 선택적 LLM 표현 변형
    → 사실·절차·적용 범위 검증
    → 설명서 Markdown + 근거 매핑 + 생성 이력 저장
    → 실제 인제션으로 청크 생성
    → 별도 질문·정답 세트로 RAG 평가
```

목표는 그럴듯한 상품 소개문이 아니라 **모델·모드·부품별 조건을 찾아서 인용할 수 있는 일관된 가상 설명서**다. 정상 설명서, 의도적으로 결함을 넣은 시험 자료, 평가 정답은 별도로 관리한다.

가상 설명서는 실제 제품 사용을 위한 자료가 아니다. 문서 제목·본문·메타데이터·검색 응답에 `가상제품 / RAG 테스트용 / 실제 사용 지침 아님`을 표시한다. 실제 브랜드의 로고·인증번호·시험기관 명의·고객센터를 만들어 붙이지 않는다. 기존 상품의 `simulated_safety_status=not_evaluated`도 유지한다.

## 2. 근거와 현재 프로젝트 상태

### 조사 결과를 생성 규칙으로 바꾸기

기준 문서는 [사용설명서 RAG 근거 조사](./유아용품_사용설명서_RAG_근거조사_2026-09-11.md)와 [가상제품 스펙 조사](./유아용품_가상제품_데이터생성용_스펙조사_2026-09-10.md)다. 아래 실제 사례는 앞선 조사에 기록된 관찰을 설계에 연결한 것으로, 이번 작업에서 새 제품 사실을 추가 조사한 것은 아니다.

| 선행 조사에서 확인한 구조 | 가상 설명서 생성에 적용할 원칙 |
|---|---|
| M1: 카시트 설치 방식·방향과 부품별 관리가 구분됨 | 방향·설치 방식·부품을 명시한 프로필로 절차 생성 |
| M2: 침대 Mini·Bed·높이별 사용 조건이 다름 | 같은 문서의 모든 제한에 모드·대상 연결 |
| M3: 최초 사용·일상 사용·소독·석회 제거가 별도 절 | 관리 주기와 사용 단계를 별도 절차로 작성 |
| M4: 분유 제조기의 청소 조건과 배출량 기준이 구체적임 | 주기·트리거·단위를 사실 원장에 등록하고 문장과 표에서 공유 |
| M5: 합본 설명서에도 모델 전용 기능이 존재함 | 제품별 기능 플래그에 따라 해당 절만 생성 |

실제 설명서에서는 **목차·조건 표현·부품별 구분이라는 구조**를 참고한다. 특정 제품의 안전 수치나 상세 설치 지침을 가져와 가상제품에 그대로 붙이지 않는다. 외부 출처는 프로필의 설계 참고 자료이며, 가상제품의 성능·안전을 입증하는 출처가 아니다.

### 현재 파일과 연결 지점

- [상품 생성기](./scripts/generate_baby_products.py)는 `products[]`, `references[]`, `metadata`를 출력하는 구조이며 제품·참조 관계 검증 함수가 있다. 새 설명서 생성기는 이 출력을 입력으로 받는 후속 단계로 설계한다.
- 기존 생성기의 `eligibility`, `components`, `specs`, `field_status`, `dataset_meta.profile_id`를 재사용한다. 값이 반복 저장되어 서로 다르면 어느 한쪽을 임의로 선택하지 않고 입력 검증에서 중단한다.
- 상품 생성기는 사용설명서의 완전한 조립·작동 절차를 제공하지 않는다. 예를 들어 `one_hand_fold=true`만으로 버튼 위치나 누르는 순서를 알 수 없다.
- 현재 확인한 작업 폴더에는 생성기가 기본 경로로 기대하는 `scripts/유아용품_가상제품_스펙사전_v1.json`이 파일 목록에 없었다. 루트의 동명 파일도 확인되지 않았다. 실행 전 실제 사전을 확보하고 `--dictionary` 경로를 확인해야 한다. 본 문서는 기존 생성기의 실행 성공이나 사용법 문서의 과거 테스트 결과를 재검증한 것이 아니다.
- [RAG 검색 코드](./src/rag/evidence_search.py)는 현재 도메인·축 중심 미니 코퍼스 목 구현이다. 모델·버전·가상 데이터 영역별 검색 격리는 별도로 구현해야 한다.

## 3. 입력 계약: 상품 스펙만으로 부족한 내용을 보완한다

### 3.1 기존 입력과 추가 입력

| 입력 | 재사용·추가 내용 | 누락 시 처리 |
|---|---|---|
| 상품 카탈로그 | 제품·옵션 ID, 브랜드·모델, 국가, 사용 범위, 스펙, 필드 상태 | 필수 식별자 누락은 생성 실패 |
| 가상 참조 데이터 | 부품, 연결 규격, 차량 좌석, 관리 프로필 등 `references[]` | 참조 ID 대상이 없으면 실패 |
| 사용 프로필 | 모드 목록, 사전조건, 사용·중단 조건, 지원·미지원 기능 | 범위가 정의되지 않은 사용법은 생성하지 않음 |
| 부품·구조 프로필 | 부품 이름·역할, 내장 부품과 별매품 구분, 연결 지점, 방향·잠금 상태 | 구성품·조립 설명의 대상이 불명확하면 실패 또는 제한 문서 |
| 절차 프로필 | 초기·전이 상태, 조작 단계, 단계별 확인, 실패 시 조치 | 조작 순서를 LLM이 보충하지 않음 |
| 관리 프로필 | 부품별 세척·소독·건조 허용, 주기, 온도·시간이 필요한 경우 그 값 | 방법만 있으면 방법 수준까지만 서술 |
| 경고 프로필 | 위험 분류, 적용 조건, 금지 행동, 연결 절차·부품 | 필수 경고가 빠지면 완전 설명서 게시 불가 |
| 문제 해결 프로필 | 증상·가상 오류 코드, 원인, 확인·조치, 중단 조건 | 등록되지 않은 오류 코드를 생성하지 않음 |
| 문서 프로필 | 언어, 절 순서, 표현 유형, 표 사용, 적용 모델, 문서 버전 | 기본 한국어 템플릿 사용 가능 |
| 생성 설정 | 시드, 생성기·프로필·템플릿 버전, 선택적 LLM 설정 | 이력 미기록 문서는 게시하지 않음 |

추가 입력은 상품마다 긴 문서를 수작업으로 작성하는 대신 **유형별 완성 프로필**로 재사용한다. 예를 들어 수동 바운서와 전동 스윙에는 서로 다른 절차 프로필을 선택한다. 품목만 같다는 이유로 모든 제품에 동일 프로필을 쓰지 않는다.

`components=[]`는 기존 계약에 따라 알려진 추가 구성품이 없다는 뜻일 수 있다. 이를 ‘제품에 버클·바퀴 등 내장 부품이 전혀 없다’로 해석하지 않는다. 내장 구조를 설명하려면 별도의 부품 프로필이 필요하다.

### 3.2 사실 원장

설명서에 들어가는 주장마다 `fact_id`를 부여한다. 사실 원장은 문장 생성과 검증이 공유하는 중간 데이터다.

| 필드 | 의미 |
|---|---|
| `fact_id` | 제품·버전 안에서 고유한 주장 ID |
| `subject` | 제품·모드·부품·설치 방식 |
| `predicate` | 최대 하중, 필요 발달, 허용 관리법, 필수 단계 등 |
| `value`, `unit`, `operator` | 값과 단위 및 `lt/lte/gt/gte/eq` 같은 비교 연산 |
| `conditions` | AND/OR 조건식, 적용 환경 |
| `status` | `known/unknown/conflicting/not_applicable` |
| `origin_kind` | `catalog/profile/derived` 중 하나 |
| `origin_ref` | 상품 JSON 포인터 또는 프로필 ID·버전·항목 ID |
| `derivation` | 계산된 값일 때 계산식과 입력 fact ID |

`known`은 가상세계에서 값이 정해졌다는 의미다. 실제 검증이나 인증 완료를 의미하지 않는다. 미확인 값을 LLM의 상식으로 채우지 않는다.

### 3.3 상태와 조건의 의미

- `known + false`: 기능을 지원하지 않음. 해당 기능 사용 절차는 제외하고 필요하면 미지원 문장을 출력한다.
- `unknown + null`: 확인되지 않음. ‘불가능’이나 ‘가능’으로 바꾸지 않는다.
- `not_applicable + null`: 해당 유형에 적용되지 않음. 예를 들어 수동 제품의 충전 절을 생성하지 않는다.
- `conflicting`: 출처별 주장을 따로 보존한다. 정상 설명서 생성에서는 해결 전 실패 처리하고, 상충 평가 세트에서만 의도적으로 사용한다.

정상 상품의 적용 조건은 정형식으로 정규화한다. 기존 `weight_over_22kg` 같은 문자열은 등록된 파서로만 변환하고, 모르는 문자열을 느슨하게 해석하지 않는다. ‘22 kg 이하’는 `lte 22`, ‘22 kg 초과 시 중단’은 `gt 22`이며 같은 값에서 판정이 뒤집히지 않아야 한다.

조건 판정은 다음 원칙을 쓴다. 하나라도 확정된 위반 또는 중단 조건이 있으면 `ineligible`; 위반은 없지만 필수 조건이 미확인이면 `unknown`; 필요한 모든 조건이 확인되고 충족되면 `eligible`이다. OR 조건은 하나가 참이면 참, 모두 거짓이면 거짓, 그 밖은 미확인으로 평가한다. 상충 주장만으로 생긴 위반은 확정 위반으로 취급하지 않는다.

## 4. 설명서 템플릿과 품목별 활성화

### 4.1 공통 절

| 절 ID | 내용 | 생성 조건 |
|---|---|---|
| S00 | 가상 표시, 모델·버전·적용 범위 | 모든 문서 필수 |
| S01 | 용도, 사용 대상·모드, 제한·중단 조건 | 모든 문서에 해당 정보 또는 명시적 미확인 상태 |
| S02 | 구성품·부품 식별 | 부품 정보가 정의된 경우 |
| S03 | 조립·설치와 완료 확인 | 조립·설치 프로필이 있는 경우 |
| S04 | 최초 사용과 매회 사용 전 점검 | 준비·점검 프로필에 따라 생성 |
| S05 | 정상 사용·조절·정지 | 지원 모드별 절차 프로필 필요 |
| S06 | 경고·금지·감독·사용 중단 | 적용 경고를 모으고 관련 절에 다시 연결 |
| S07 | 호환 제품·부속품·설치 환경 | 등록된 호환 관계 또는 미확인 상태 |
| S08 | 부품별 세척·소독·건조·교체 | 관리 프로필에 따라 생성 |
| S09 | 증상·원인·조치 | 문제 해결 프로필이 있는 경우 |
| S10 | 보관·운반·폐기 | 관련 프로필에 따라 생성 |
| S11 | 가상 지원 정보·문서 적용 범위 | 실제 연락처 없이 테스트 자료임을 설명 |

필요한 절이 정의되지 않으면 `coverage_status=partial`과 `missing_sections`를 기록한다. 모든 절에 ‘정보 없음’을 채워 넣고 완전 설명서라고 부르지 않는다. `coverage_status=complete`는 선택된 템플릿의 요구 항목이 충족된다는 뜻이며 실제 안전성이나 법정 요건 충족을 의미하지 않는다.

### 4.2 23개 품목군의 생성 모듈

| 품목 | 추가 프로필·분기 | 생성 시 검증할 관계 |
|---|---|---|
| 유모차 `stroller` | 접기·펼치기, 좌석·배시넷, 브레이크·하네스 | 신생아 구성 미지원이면 신생아 사용 절차 없음; 좌석·바구니 하중 분리 |
| 카시트 `car_seat` | 방향×고정 방식×가상 차량 좌석별 설치 | 지원 방향·앵커·필수 부품·차량 호환이 모두 일치 |
| 아기띠 `carrier` | 자세별 착용·확인 프로필 | 지원하지 않는 앞보기·뒤업기 절차 없음 |
| 침대 `crib` | 높이·매트리스·난간 전환·조립 | 모드별 중단 조건·규격 일치 |
| 슬립색·속싸개 `sleepwear` | 유형별 착의·발달 조건 | 팔 고정 여부와 전환·중단 경고 일치 |
| 바운서·스윙 `bouncer` | 수동·전동, 바운서·의자 모드 | 수면 용도·모드 하중·전원 분기 일치 |
| 하이체어 `high_chair` | 착석·발판·트레이·신생아 구성 | 부속품 없는 모드 활성화 금지 |
| 젖병 `bottle` | 병·젖꼭지·링별 조립·관리 | 소독 허용을 다른 부품에 확장하지 않음 |
| 유축기 `pump` | 플랜지·연결계, 수동·전동 작동 | 모터·튜브·모유 접촉 부품 관리 구분 |
| 소독기·세척기 `sterilizer` | 기능별 코스·적재·관리 | 세척 미지원이면 세척 기능 설명 없음; 수용량의 기준 용기 유지 |
| 분유 제조기·포트 `formula_maker` | 기기 유형·설정·급수·청소 | 설정 참조와 기기 모델 일치; 조유 비율 임의 생성 금지 |
| 분유 `formula` | 가상 포장 사용 표시 | 비율·온도·보관 기한은 별도 정의 없으면 미제공 |
| 이유식 `baby_food` | 가상 용기·가열·보관 표시 | 가열 방식·용기·보관 상태 일치 |
| 컵 `cup` | 뚜껑·밸브·빨대·열린 컵 분기 | 부품 수·분해 절차·세대 호환 일치 |
| 턱받이 `bib` | 섬유·실리콘별 착용·관리 | 소재명만으로 세탁 온도 생성 금지 |
| 기저귀 `diaper` | 테이프·팬티형 착용·폐기 표시 | 체중 구간과 유형 일치 |
| 물티슈 `wipes` | 사용 부위·밀봉·보관 표시 | 정의되지 않은 살균·치료 기능 추가 금지 |
| 욕조 `bath` | 지지대·배수·접기·보관 | 대상 모드와 부속품·하중 연결 |
| 세정제·보습제 `skincare` | 사용 부위·씻어내기·보관 표시 | 세정제와 보습제 사용법 혼동 방지 |
| 체온계 `thermometer` | 측정 방식·필터·표시·오류 | 지원 부위·기능만 출력; 임상 진단 기준 임의 생성 금지 |
| 공갈젖꼭지 `pacifier` | 점검·관리·교체 표시 | 사용 단계·부품 관리 일치 |
| 안전문·베이비룸 `gate` | 압착·고정·패널형 설치 | 설치 면·폭·확장품·위치 조건 일치 |
| 매트 `mat` | 설치·접기·청소·건조 | 정의되지 않은 차음 시험·수면 적합성 추가 금지 |

분유·이유식·기저귀·물티슈·화장품은 장문의 기계 설명서보다 **포장 사용 표시 형식**을 기본으로 한다. 제품 특성에 맞지 않는 조립·고장 코드 절을 길이 확보 목적으로 추가하지 않는다.

## 5. 절차 프로필을 만드는 방법

### 5.1 상태 전이로 절차를 정의한다

‘버튼을 눌러 펼친다’라는 문장만으로는 순서·완료 조건·실패 처리가 정의되지 않는다. 프로필에는 다음을 포함한다.

```text
profile_id / profile_version / applicable_categories / compatible_features
parts[]: id, name, role
states[]: 접힘, 펼침_미잠금, 펼침_잠금 등 가상 설계 상태
steps[]:
  step_id
  requires_state
  required_parts
  action_code
  resulting_state
  confirmation
  warning_ids
  on_failure
terminal_states[]
```

이 구조는 모든 제품의 실제 기구를 모델링하는 물리 시뮬레이션이 아니다. **가상 설명서 안에서만 일관된 구조·작동 정의**다. 등록된 부품과 상태에만 지시를 생성하며, 도달 불가능한 단계·순환·누락된 완료 상태를 검사한다. 반복 청소 코스처럼 의도된 반복은 종료 조건을 명시한다.

### 5.2 프로필 작성과 검수 범위

1. 해당 품목의 조사 체크리스트에서 필요한 절을 선택한다.
2. 가상 제품 유형의 기능·부품을 정의하고 지원하지 않는 모드를 제거한다.
3. 관리·경고·절차를 항목 ID가 있는 구조화 데이터로 작성한다.
4. 제품 속성에 연결할 변수와 프로필 고정값을 구분한다.
5. 연결 무결성·수치·상태 전이를 검증하고 사람이 의미를 검수한다.
6. 검수된 프로필에 버전을 부여해 여러 제품에 재사용한다.

사용 한도, 조유, 식품 보관, 측정 해석 등은 기존 가상 프로필에 정의된 정보 범위까지만 생성한다. 상세 조제 비율 등이 없으면 이를 추가로 발명하지 않고 해당 설명서의 완성도를 제한한다. 이런 제한을 보완하려면 별도의 프로필을 명시적으로 설계·검수해야 한다.

## 6. 생성기의 역할과 LLM의 역할

| 항목 | 규칙 기반 처리 | LLM에 허용할 일 |
|---|---|---|
| 모델·버전·부품 ID | 원본에서 복사 | 없음 |
| 수치·단위·조건·금지 | 사실 원장으로 문장·표 렌더링 | 원칙적으로 고정 구간 편집 금지 |
| 사용 순서 | 절차 프로필 순서를 유지 | 승인된 단계 안에서만 표현 조정 |
| 경고와 예외 | 필수 문장으로 출력, 관련 단계에 연결 | 새 경고·예외 생성 금지 |
| 제목·문체 | 승인된 제목 집합·표현 유형 선택 | 의미를 유지하는 표현 변형 |
| FAQ | 질문 틀과 사실 ID로 구성 | 질문의 자연스러운 바꿔쓰기 |
| 그림 | 부품·상태 데이터 기반 도식 | 이미지의 미정 구조를 사실로 확정하지 않음 |
| 적합성 정답 | 조건식 평가기로 계산 | 판정 결과를 설명하는 문장 작성만 가능 |

### 권장 생성 모드

- **기본 모드:** LLM 없이 템플릿으로 전체 문서를 출력한다. 개발 초기의 재현 가능한 기준 데이터다.
- **표현 변형 모드:** 검증된 기본 문서의 허용 구간만 바꾼다. 결과는 별도의 문서 변형 ID를 가지며 사실 원장은 동일하다.
- **문서 형식 변형 모드:** 절 순서·표현 방식·표와 목록 배치를 바꾼다. 사실 ID는 유지하고 원문 위치 매핑은 다시 계산한다.

LLM이 문서 전체를 자유롭게 다시 쓰게 하면 ‘22 kg 이하’를 ‘22 kg 미만’으로 바꾸거나 조건을 생략할 수 있다. 초기 구현에서는 중요한 문장을 고정 문자열로 삽입하고, 나머지 문장만 생성하도록 하는 방식이 적합하다.

### 표현 변형용 프롬프트 계약 예시

```text
역할: 가상제품 설명서의 한국어 문장 편집기.
입력: section_id, 고정 문장, 허용 사실 ID와 값, 편집 허용 문장.

규칙:
- 고정 문장과 단계 순서를 변경하지 않는다.
- 입력에 없는 기능·부품·수치·금지·예외·연락처를 추가하지 않는다.
- known/unknown/not_applicable 상태를 바꾸지 않는다.
- 모든 출력 블록에 사용한 fact_ids를 반환한다.
- 작성에 필요한 정보가 없으면 missing_fact_ids와 이유를 반환한다.
- 실제 제품 인증·안전 검증을 암시하지 않는다.

출력: section_id, blocks[{text, fact_ids}], missing_fact_ids.
```

모델이 붙인 `fact_ids`는 검증 결과가 아니다. 후속 검사에서 해당 사실이 실제로 문장을 뒷받침하는지 확인해야 한다.

## 7. 제품 1건의 생성 예시

아래는 기존 가상 스펙 문서의 `SYN-STROLLER-001` 예시를 사용한 **부분 설명서 설계 시연**이다. 새 절차 프로필 없이 입력에서 확정할 수 있는 내용만 생성한다. 실제 상품도, 완성된 설명서도 아니다.

### 7.1 입력 사실과 필요한 추가 정보

| 사실 ID | 입력 경로 | 값 |
|---|---|---|
| F01 | `eligibility[0].min_age_months` | 6 |
| F02 | `eligibility[0].max_weight_kg` 및 `specs.seat_max_kg` | 22 kg, 두 값 일치 필요 |
| F03 | `eligibility[0].requires` | `independent_sitting` |
| F04 | `specs.basket_max_kg` | 3 kg |
| F05 | `specs.newborn_setup` | `not_supported` |
| F06 | `specs.one_hand_fold` | true |
| F07 | `specs.self_standing_folded` | false |
| F08 | `eligibility[0].stop_when_any` | `weight_over_22kg` |

추가 정보가 필요한 항목: 접기 조작 순서, 잠금 확인 방법, 브레이크·하네스 구조, 부품별 세척, 점검·보관 절차. F06이 true라는 사실만으로 접기 버튼의 위치나 조작법을 생성하면 실패다.

### 7.2 생성되는 부분 설명서

> **가상제품 / RAG 테스트용 / 실제 사용 지침 아님**  
> 가상브랜드_새봄 SYN-LITE-A · SYN-STROLLER-001 · KR_DEMO  
> 문서 SYN-MAN-STROLLER-001-R1 · 일부 항목만 정의된 테스트 문서
>
> **사용 대상**  
> 좌석 모드는 생후 6개월 이상, 체중 22 kg 이하이며 혼자 앉을 수 있는 아이를 대상으로 설정되어 있습니다. 체중이 22 kg을 초과하면 좌석 모드 사용을 중단하는 조건입니다. 신생아용 구성은 지원하지 않습니다.
>
> **수납과 접기 기능**  
> 바구니의 최대 하중은 3 kg입니다. 이 한도는 좌석의 아이 체중 한도와 별개입니다. 한 손 접기 기능은 지원하지만, 접힌 상태로 스스로 서는 기능은 지원하지 않습니다.
>
> **미제공 항목**  
> 접기 조작 순서와 부품별 관리 방법은 이 테스트 문서에 정의되지 않았습니다.

미제공 항목 안내는 정상 완전 문서의 공통 문구가 아니다. 이 부분 문서의 적용 범위를 나타내는 표시다. ‘정보 미제공’과 ‘실제 검색 자료에서 정보를 제거한 누락 평가’는 별도로 다룬다.

### 7.3 근거 매핑 예시

```json
{
  "manual_id": "SYN-MAN-STROLLER-001",
  "revision": "R1",
  "product_id": "SYN-STROLLER-001",
  "market": "KR_DEMO",
  "is_synthetic": true,
  "coverage_status": "partial",
  "missing_sections": ["S03", "S04", "S05", "S08", "S09", "S10"],
  "blocks": [
    {"block_id": "S01-B01", "fact_ids": ["F01", "F02", "F03", "F08"]},
    {"block_id": "S01-B02", "fact_ids": ["F05"]},
    {"block_id": "S07-B01", "fact_ids": ["F04"]},
    {"block_id": "S05-B01", "fact_ids": ["F06", "F07"]}
  ],
  "notice": "가상제품 / RAG 테스트용 / 실제 사용 지침 아님"
}
```

이 JSON은 핵심 필드 예시이며 완전한 JSON Schema는 아니다. S05에는 기능 설명 블록이 있지만 정상 사용 절차는 없으므로 `missing_sections`에 S05가 남는다. 실제 구현에서는 절별 `complete/partial/not_applicable` 상태도 기록한다.

### 7.4 별도 사용자 조건으로 정답 만들기

| 사용자 조건 | 좌석 사용 범위에 대한 기대 판정 | 필요한 사실 |
|---|---|---|
| 10개월·10 kg·혼자 앉음 | eligible | F01~F03, F08 |
| 10개월·22 kg·혼자 앉음 | eligible: 상한 포함 확인 | F01~F03, F08 |
| 10개월·22.1 kg·혼자 앉음 | ineligible | F02, F08 |
| 10개월·10 kg·혼자 앉기 미확인 | unknown | F03 |
| 5개월·10 kg·혼자 앉음 | ineligible | F01 |

이는 **정의된 가상 좌석 사용 조건만의 판정**이다. 설치·제품 안전·예산까지 확인된 최종 추천 판정으로 확대하지 않는다.

## 8. 검증과 재생성

### 8.1 자동 검사

| 단계 | 검사 내용 | 실패 예 |
|---|---|---|
| 입력 계약 | 타입, 필수 필드, 상태와 null, 중복 ID | unknown인데 임의 값 존재 |
| 참조 관계 | 부품·프로필·호환 대상 존재, 적용 범위 | 본문에 없는 부품이 단계에 등장 |
| 수치·조건 | 상하한·단위·경계·모드별 반복값 일치 | 22 kg 상한을 25 kg으로 변경 |
| 기능 분기 | 미지원 기능·모드의 절이 없는지 | 수동 제품에 충전 안내 생성 |
| 절차 | 사전조건·단계 순서·완료 확인·실패 조치 | 잠금 전에 사용 완료 처리 |
| 문장·표 | 고정 문장 일치, 각 주장에 근거 연결 | 표는 22 kg인데 FAQ는 20 kg |
| 경고·예외 | 필수 경고 ID의 누락, 조건·부정어 변화 | ‘사용하지 마십시오’에서 부정어 삭제 |
| 적용 정보 | 모델·국가·버전·합성 표시 | 다른 제품 ID 또는 실제 브랜드 표기 |
| 추적 가능성 | 모든 매핑이 실제 블록·문서 해시와 연결 | 존재하지 않는 절을 정답 근거로 지정 |

수치 검사는 문서의 모든 숫자를 무조건 값으로 취급하지 않는다. 모델번호·문서번호·단계 번호·페이지 번호를 구별하고 **주장 단위의 값과 연산자**를 비교한다. LLM 재추출은 보조 검사로 쓸 수 있지만, 같은 모델의 자기평가만으로 통과시키지 않는다.

### 8.2 사람의 검수와 통과 기준

새 프로필·템플릿은 최초 게시 전에 의미를 검수한다. LLM이 바꾼 조건·안전 관련 문장은 검수 완료 전 게시하지 않는다. 그 밖의 표현 변형은 자동 검사와 표본 검수로 관리할 수 있다.

권장 게시 기준은 필수 사실·경고 누락 0건, 근거 없는 주장 0건, 단위·조건·참조 불일치 0건이다. 이는 설계 목표이며 현재 측정된 성능이 아니다. 텍스트 일관성 검사 통과와 실제 제품 안전 검증은 구분한다.

### 8.3 실패 복구

- 입력·프로필 오류: 생성 중단 후 원본 또는 프로필 수정. LLM에게 모순을 알아서 해결하도록 하지 않는다.
- 표현 변형 오류: 해당 절만 원래 사실을 유지해 최대 2회 재생성한다는 초기 정책을 둘 수 있다. 횟수는 설정값으로 관리한다.
- 반복 실패: 검증된 기본 템플릿 문장으로 되돌린다. 그 기본 문장도 검증되지 않으면 격리한다.
- 검증 실패 문서는 검색 게시 목록에 넣지 않는다. 오류 보고서와 재시도 이력은 남긴다.

## 9. RAG 코퍼스와 평가 데이터 생성

### 9.1 정답과 문서를 분리한다

다음 세 영역을 분리한다.

1. **기준 데이터:** 제품·참조·프로필·사실 원장. 정답 계산의 기준이다.
2. **검색 문서:** 설명서, 가상 부속품 안내, 필요한 경우 가상 판매자 주장. RAG가 접근하는 데이터다.
3. **평가 전용 데이터:** 사용자 질문, 조건, 기대 판정, 필수 사실 ID, 허용 근거 블록, 금지 주장. 검색 인덱스에 넣지 않는다.

문서 생성기가 쓴 답변을 그대로 정답으로 저장하지 않는다. 정답은 사실 원장과 사용자 조건을 별도 규칙 평가기로 계산한다. 질문은 사실 ID와 질문 유형에서 만들고, 자연어 변형은 그다음 적용한다.

추천 평가에서 예산·배송·가격은 별도 `offer` 자료를 사용한다. 설명서에 현재 판매가를 넣어 정답 근거가 되게 하지 않는다.

### 9.2 정상·어려운 사례·결함 자료

| 유형 | 만드는 방법 | 기대 동작 |
|---|---|---|
| 단일 사실 | 한 모델의 한 절에서 찾을 수 있는 질문 | 정확한 블록을 인용 |
| 조건 결합 | 월령·체중·발달을 함께 요구 | 모든 필수 조건 대조 |
| 경계값 | 상한과 그 직전·직후 조건 | 포함·제외 경계 준수 |
| 부품 차이 | 같은 제품 내 서로 다른 관리 규칙 | 대상 부품만 답변 |
| 모드 차이 | 같은 제품의 모드별 조건 차이 | 다른 모드의 상한 혼입 금지 |
| 모델·버전 유사 | 유사 모델명, 구버전, 다른 국가 문서 추가 | 적용되는 문서만 선택 |
| 다중 문서 | 본체 + 어댑터 + 호환표 | 필요한 관계를 함께 확인 |
| 정보 누락 | 정상 문서에서 목표 사실 및 동의 표현을 제거 | 해당 사실을 묻는 질문에 미확인 |
| 사용자와 부적합 | 정상 문서는 유지하고 사용자 조건만 변경 | 이유와 함께 제외 |
| 출처 간 상충 | 같은 대상에 다른 가상 출처의 충돌 주장 추가 | 정책에 따라 출처 차이 설명·보류 |
| 문서 결함 | 숫자·부정어·표 셀 등을 의도적으로 변형 | 인제션·검증 단계에서 검출 |

정보 누락 케이스에서는 원래 정답을 시스템 내부에만 보존하고 `expected_answerability=unanswerable`로 설정한다. 현재 검색 문서에 없는 사실을 모델이 기억하거나 추측해 맞혔더라도 근거 기반 정답으로 인정하지 않는다. 본문·FAQ·표·청크 메타데이터에 같은 사실이 남았는지도 검사한다.

의도적 결함은 `fixture_type`, `parent_artifact_hash`, `mutation_rule_id`, `changed_locations`, `expected_failure`로 기록한다. 결함 표시와 정답은 평가 전용 영역에 두고 검색 본문에 답을 노출하지 않는다. 정상 설명서 코퍼스와 별도 게시 경로를 사용한다.

### 9.3 데이터 분할과 측정

같은 제품의 설명서 표현 변형·구버전·누락본은 동일한 분할 그룹에 둔다. 변형본이 학습과 시험에 나뉘어 정답이 유출되지 않게 한다. 별도로 미사용 프로필·미사용 문서 형식의 평가 세트를 만들어 새로운 구조에 대한 성능을 측정한다.

| 지표 | 정의 |
|---|---|
| 필수 근거 회수율@k | 질문에 필요한 gold 사실 중 상위 k개 검색 결과가 포함한 사실 비율 |
| 완전 근거 회수 성공률@k | 필요한 사실 전체를 상위 k개 결과가 포함한 질문 비율 |
| 적용 범위 오류율 | 다른 제품·모드·국가·버전 근거를 사용한 답변 비율 |
| 판정 정확도 | eligible/ineligible/unknown 기대 판정과 일치한 비율; 유형별 보고 |
| 인용 뒷받침 비율 | 검토한 답변 주장 중 연결된 인용이 실제로 뒷받침한 주장 비율 |
| 미확인 처리 | 답변 불가 질문을 보류한 비율과 답변 가능 질문을 불필요하게 보류한 비율 |
| 결함 검출률 | 주입한 결함 중 게시 전에 발견한 비율 |

시험 세트에 별도 정답 매핑을 갖되, 실제 인제션이 만든 청크 경계로 위치를 다시 연결한다. 예상 청크를 직접 검색 결과로 주입하는 방식은 검색 품질 평가로 간주하지 않는다. Markdown 단계는 내용 검색 평가, 이후 PDF 단계는 추출·레이아웃을 포함한 평가로 분리한다.

## 10. 산출물·버전·저장 구조

다음은 **구현 시 생성할 파일 구조 제안**이다. 이번 작업에서 해당 파일들이 만들어졌다는 뜻은 아니다.

```text
generated/synthetic_manuals/<run_id>/
  manifest.json
  canonical/
    products.snapshot.json
    references.snapshot.json
    profiles.snapshot.json
    facts.jsonl
  manuals/
    <manual_id>/<revision>/<variant_id>.md
  mappings/
    <manual_id>/<revision>/<variant_id>.json
  validation/
    report.json
  publish/
    accepted_manifest.json

evaluation/<dataset_version>/              # 검색 인덱싱 대상 밖
  questions.jsonl
  gold.jsonl
  split_manifest.json
  mutations.jsonl
```

`manifest`에는 입력 해시, 생성기·사전·프로필·템플릿 버전, 난수 시드, 언어, 문서 수, 합성 표시, 실행 결과를 기록한다. LLM 사용 시 모델 식별자, 프롬프트 해시, 생성 설정, 실제 응답 원본과 해시도 남긴다. 비밀키는 기록하지 않는다.

같은 시드만으로 LLM 출력이 항상 같다고 보장하지 않는다. 규칙 기반 출력은 고정 입력에서 재현성을 검사하고, LLM 변형은 저장한 실제 응답을 재생하는 방식으로 재현한다. 콘텐츠 해시 계산은 UTF-8·줄바꿈·키 순서 등 정규화 규칙을 고정한다.

제품 사실 또는 적용 프로필이 바뀌면 설명서 revision을 올린다. 표현만 바뀐 경우 별도 `variant_id`를 부여한다. 원문 내용이 달라졌으므로 각각 별도 파일 해시와 위치 매핑을 가진다. 과거 파일을 덮어써 기존 인용 위치를 깨뜨리지 않는다.

초기 출력은 Markdown으로 한다. 페이지가 없으므로 절·블록 ID와 최종 문자 위치로 인용한다. PDF를 추가할 경우 최종 렌더링 후 실제 페이지·인쇄 쪽수·도해 위치를 계산하고 시각 검수한다. 예측한 페이지 번호를 미리 넣지 않는다.

## 11. 기존 DB·검색과의 연결

기존 [DB 명세](./docs/db/table_spec.md)의 원본→자료 버전→청크→검색 결과→인용 연결을 재사용하되, 합성 데이터 여부를 문서와 검색 단계까지 유지해야 한다.

| 영역 | 적용 방향 |
|---|---|
| `assets.file_object` | 직접 생성한 설명서 원본·해시·사용 정책 저장 |
| `assets.product_material` | 가상 설명서 유형과 논리적 문서 ID 연결 |
| `assets.material_revision` | 개정·언어별 파일과 취득·생성 이력 연결 |
| `assets.material_applicability` | 가상 제품·옵션·KR_DEMO 적용 범위 연결 |
| `rag.ingestion_job` | 최종 파일을 실제 파서로 처리, 실패 기록 |
| `rag.document_chunk` | 실제 원문 위치·절·블록과 사실 매핑 연결 |
| `evidence.evidence` | 가상 원문을 인용하고 합성 표시를 응답까지 전달 |

`is_synthetic`, `dataset_id`, 프로필 버전, 사실 매핑을 어디에 둘지는 스키마 계약을 추가로 정해야 한다. 명세에 없는 컬럼이 이미 존재한다고 가정하지 않는다. 데모에서는 전용 데이터셋·저장 영역을 사용하고, 운영 검색은 허용 데이터셋을 명시적으로 제한한다. 문서 머리말에 ‘가상’이라고 쓰는 것만으로 검색 격리가 구현되지는 않는다.

가상 설명서의 출처는 자체 문서 ID·원본 경로다. 실제 제조사 URL을 가상 제품의 인용 링크로 재사용하지 않는다. 공식 설명서 조사 링크는 생성 이력의 `design_references`에만 연결한다.

## 12. 단계별 구현 계획과 완료 조건

### 1단계: 규칙 기반 부분 문서

유모차·젖병·기저귀·컵부터 시작한다. 기존 상품 생성기의 입력 사전을 확보하고 경로를 정리한 뒤, 상품·참조 데이터를 검증해 사실 원장을 만든다. 현재 입력만으로 가능한 절을 생성하고 미정의 절을 명시한다.

완료 조건: 4개 품목의 사실 매핑, 조건·부품·단위 일치, JSON 상태 구분, 같은 입력의 재현 가능한 출력. 부분 문서를 완전 문서로 게시하지 않는다.

### 2단계: 유형별 완전 프로필

조립·사용·관리·경고·문제 해결 프로필을 추가한다. 하나의 지원 유형을 완성한 뒤 다른 유형으로 확장한다. 카시트·침대·아기띠 등은 적용 모드·부품·절차가 정의된 프로필부터 지원한다.

완료 조건: 지원 유형별 요구 절 충족, 미지원 기능의 절 미생성, 절차 상태 전이 및 경고 연결 검수. 23개 품목 이름을 지원하는 것과 모든 하위 유형을 완성한 것은 구분한다.

### 3단계: RAG 평가 세트

첫 파일럿 규모 제안은 4개 품목×3개 가상 모델=12개 기본 문서와 모델당 6개 질문=72개 질문이다. 표현 변형·누락·경계 사례는 별도 개수를 기록한다. 질문 수를 채우려고 부품·기능을 임의로 추가하지 않고, 입력이 지원하는 유형만 생성한다.

완료 조건: 평가 정답 격리, 실제 인제션 기반 근거 매핑, 모델·모드 혼동 및 미확인 처리 측정. 이 수량은 계획값이며 생성 완료 수량이 아니다.

### 4단계: 표현·레이아웃 다양화

기본 모드가 통과한 후 LLM 표현 변형과 PDF·도해 형식을 추가한다. 변형마다 사실 보존 검증과 위치 재계산을 한다.

완료 조건: 중요 주장 변화 0건, 인용 위치 유효, 분할 간 변형본 유출 없음. PDF는 추출 결과와 렌더링을 모두 확인한다.

이번 산출물은 위 방법과 구현 순서를 정의한 설계 문서다. 설명서 생성기 구현, 프로필 전수 작성, 대량 설명서 생성 및 RAG 성능 측정은 포함하지 않는다.
