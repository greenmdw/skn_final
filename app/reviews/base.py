"""
`ReviewSource` — 3단계가 리뷰에게 묻는 것 하나.

[왜 포트가 필요한가 — 2026-09-08 기준]
기획안 §11은 리뷰 소스로 **네이버 강화 API**(스토어 리뷰 + 상품 Q&A)를 적어
뒀는데, 9/7 23시 회의(`cmtrc9s2300ejzvkaa02inp5w`)에서 두 사람이 각각 확인한
결과 **네이버·카카오 어느 쪽에서도 리뷰를 가져올 수 없었다.** 기획의 심장인
3단계가 데이터원 없이 남은 상태다.

그래서 기본 구현을 `synthetic` 으로 둔다. `CATALOG_SOURCE=snapshot` 이 키 없이
실데이터로 도는 것과 같은 배치다 — **아무것도 설정하지 않아도 엔진 전체가 돈다.**
실데이터 소스가 생기면 이 포트를 만족하는 파일을 하나 더 놓으면 된다.

합성의 값은 기획안 §11이 이미 적어 뒀다: *"조작 리뷰 판별은 정답 라벨이
필요한데 합성이면 라벨이 공짜다."* 다만 같은 문단의 단서도 그대로 지킨다 —
**합성임을 숨기지 않는다.** 그래서 `name` 이 응답에 실려 나간다.
"""

from __future__ import annotations

from typing import Protocol

from ..engine.schemas import Claim, Evidence

# 이 건수 미만이면 판정하지 않는다 — `verify.py` 의 NO_EVIDENCE 임계값이 쓴다.
# 상수를 여기 두지 않고 verify 쪽에 둔 이유는, 이게 소스의 성질이 아니라
# **판정 규칙**이기 때문이다. 소스를 바꿔도 임계값은 그대로여야 한다.


class ReviewSource(Protocol):
    name: str
    # 이 소스의 원문을 응답에 실어도 되는가. **기본은 안 된다.**
    # 9/8 17시: 리뷰 원문을 그대로 가져와 저장·노출하는 것은 법적 문제가 있다.
    # 우리가 만든 합성 데이터만 예외다. `Review.text` 도 대조하는 동안에만 쓰는
    # 값이고 저장 대상이 아니다 — 실소스 어댑터는 요청이 끝나면 버려야 한다.
    may_quote: bool = False

    def evidence_for(self, claim: Claim) -> Evidence:
        """
        주장 하나에 대해 표본과 **어긋난 언급 수**를 돌려준다.

        확인 쪽을 세지 않는다 — 침묵이 확인의 근거다. 목업도 *"반증 사례 없음"*
        을 확인 판정의 근거로 쓴다.
        """
        ...

    def risk_distribution(self) -> dict[str, int]:
        """
        대조에 쓴 리뷰 전체의 **조작 확률 분포**.

        점수 하나가 아니라 구간별 건수다. 기획안 §7이 이진 판정을 하지 않기로
        한 이유가 §6-3에 있다 — Fakespot·ReviewMeta 가 무너진 것이 개별 리뷰의
        진위를 맞히려 했기 때문이고, 우리는 진위를 목적이 아니라 수단으로 쓴다.
        """
        ...
