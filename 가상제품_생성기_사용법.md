# 유아용품 가상 제품 생성기

Python 3.10 이상에서 실행한다. 외부 패키지·API 키·네트워크 연결은 필요 없다.

## 파일

- `generate_baby_products.py`: 생성 함수, 데이터 검증 함수, 명령행 실행 기능.
- `유아용품_가상제품_스펙사전_v1.json`: 23개 품목군·189개 품목별 필드의 기존 사전. 스크립트와 같은 폴더에 둔다.
- `test_generate_baby_products.py`: 재현성·모든 품목 생성·조건부 필드·오류 검출·CLI 테스트.
- `generated/baby_products.json`: 시드 42로 생성한 상품 100개와 참조 레코드 78개의 실행 예시.

## 실행

프로젝트 폴더에서 다음 명령을 실행한다.

```powershell
# 총 100개: 전체 23개 품목을 순서대로 순환하여 배분
python generate_baby_products.py --count 100 --seed 42

# 특정 품목만 총 30개 생성
python generate_baby_products.py --categories stroller,bottle,diaper --count 30 --seed 7 --output generated/selected.json

# 이미 존재하는 출력 파일을 의도적으로 교체할 때
python generate_baby_products.py --count 100 --seed 42 --force

# 테스트
python -m unittest -v test_generate_baby_products
```

기본 예시 파일은 이미 생성되어 있으므로 첫 명령을 다시 실행할 때 다른 `--output`을 지정하거나 `--force`를 사용한다. 기본 동작은 기존 파일 덮어쓰기를 거부한다. `--count`는 품목당 개수가 아닌 **전체 상품 수**다. 전체 품목을 한 번 이상 생성하려면 23 이상으로 지정한다. 100개이면 앞의 8개 품목은 5개씩, 나머지 15개 품목은 4개씩 생성한다.

지원 품목 ID:

```text
stroller,car_seat,carrier,crib,sleepwear,bouncer,high_chair,bottle,pump,
sterilizer,formula_maker,formula,baby_food,cup,bib,diaper,wipes,bath,
skincare,thermometer,pacifier,gate,mat
```

`--dictionary`로 사전 경로를 바꿀 수 있다. 기존 23개 품목 구조를 전제로 하므로 새 품목이나 필드를 추가하려면 코드의 프로필과 검증 로직도 함께 수정한다. 모든 자연어 제약을 자동 해석하는 생성기는 아니다.

## 출력 구조

```text
metadata
  generator_version, seed, count, category_counts, dictionary_sha256
  is_synthetic, profile_scope, notice, scenario_scope
products[]
  product_id, is_synthetic, brand, model, category_id, variant_id, market
  eligibility[], components[], specs, field_status
  simulated_safety_status, offer, dataset_meta
references[]
  id, kind, is_synthetic, ...참조 종류별 값
```

상품은 기존 레코드 예시의 구조를 유지한다. `references`에는 상품이 참조하는 가상 부품·차량 좌석·연결 규격·관리/시험 프로필 등이 들어간다. 참조 ID만 있고 대상 레코드가 없는 경우 검증에서 실패한다.

가격·배송·재고는 `offer`에 둔다. 배송 리드타임은 `lead_time_basis="dispatch"`로 **출고까지의 일수**임을 명시하며 도착 보장으로 해석하지 않는다. 포장 제품 가격은 해당 레코드에 명시된 포장 구성 전체의 가상 판매가다.

`components[].included=false`이면서 선택한 모드가 `required_for_modes`에 있으면 해당 참조 레코드의 `price_krw`를 별매 필수 비용으로 계산할 수 있다. 본체와 구성품을 별도 저장하며 총구매안 계산기는 포함하지 않는다.

## 코드에서 직접 사용

```python
from generate_baby_products import generate_dataset, load_dictionary, validate_dataset

dictionary = load_dictionary()
dataset = generate_dataset(
    count=30,
    seed=7,
    categories=["stroller", "bottle", "diaper"],
    dictionary=dictionary,
)
validate_dataset(dataset, dictionary)
products = dataset["products"]
```

같은 사전 내용·코드 버전·시드·개수·품목 순서이면 동일한 결과를 생성한다. 실행 시각을 데이터에 넣지 않으므로 동일 입력의 JSON 출력도 재현된다. 파이썬 표준 난수기를 사용하므로 서로 다른 Python 버전 간 바이트 단위 재현까지 보장하지 않는다.

## 생성 방식

1. 사전에서 숫자·열거형 후보를 선택한다. 배열은 배열 전체를 사용하고 객체는 예시 객체 하나를 선택한다.
2. 연관 속성을 `apply_profile()`에서 함께 확정한다. 예: 수동 유축기는 배터리·전동 흡입 단계를 해당 없음 처리한다.
3. 호환 부품과 참조 프로필을 상품 ID 기반으로 생성한다. 상품 간 우연한 ID 충돌을 막는다.
4. 사용 조건과 제품 스펙의 반복 값은 같게 만들되 객체를 복사해 수정 시 서로 따라 바뀌지 않도록 한다.
5. 자료형·범위·필수 키·참조 무결성과 품목별 주요 관계를 검증한 뒤 출력한다.

`field_status=known`은 가상 프로필에 값이 정의되었다는 뜻이다. 조건상 해당 없는 필드는 `null + not_applicable`이며, 매트의 제공하지 않는 소음 시험 정보는 `null + unknown`이다. 모든 상품의 실제 안전 평가 상태는 `not_evaluated`로 유지한다.

## 현재 지원 범위

23개 품목군을 모두 생성하되 각 품목의 모든 하위 유형을 생성하지는 않는다. 예를 들어 유모차는 휴대형·일반형, 아기띠는 구조형, 침대는 일반 아기침대, 카시트는 가상 ISOFIX 후방형 두 프로필을 지원한다. 전방형·부스터·신생아 힙시트처럼 별도 조건이 필요한 조합은 임의로 만들지 않는다. 관련 프로필은 코드에서 명시적으로 추가한다.

이 구현은 **정상 상품 카탈로그 생성기**다. 가격·재고가 사용자 조건에 맞는지는 별도 판정 대상이다. 사용자 시나리오, 추천 정답, 상충 주장 및 의도적 결함 주입은 생성하지 않는다. `dataset_meta.scenario_id`는 그래서 `null`이다.

가격 범위·사용 한도·원재료 예시 등은 테스트용 설계값이며 시장 통계가 아니다. 가상 식품 프로필은 실제 제조 처방이나 보관 지침이 아니고, 분유 프로필의 물·분말 비율은 제공하지 않는다. 인증번호·실제 시험 성적·사용 후기를 생성하지 않는다. 검증 함수는 데이터 일관성 검사이며 제품 안전성 검사가 아니다.

## 검증 결과

`python -m unittest -v test_generate_baby_products`의 10개 테스트 통과. 여러 시드에서 총 2,300개 생성 검증, 동일 시드 재현성, 필터 배분, 원본 사전 불변성, JSON 저장·덮어쓰기 방지, 다른 작업 폴더에서 CLI 실행을 확인한다. 의도적으로 주입한 잘못된 사용 한도·0 치수·없는 부품 ID·뒤집힌 체중 범위·누락 알레르겐·바운서 수면 용도·매트리스 치수 불일치도 검출한다.
