# 가상 유모차 레코드 스펙표

원본: `유아용품_가상제품_레코드예시_v1.json`. 아래 값은 모두 테스트용 가상 데이터다. 치수 순서는 가로 × 깊이 × 높이다.

| 구분 | 항목 | 값 | 원본 필드 |
|---|---|---|---|
| 기본 정보 | 상품 ID | SYN-STROLLER-001 | `product_id` |
| 기본 정보 | 브랜드 | 가상브랜드_새봄 | `brand` |
| 기본 정보 | 모델 | SYN-LITE-A | `model` |
| 기본 정보 | 품목 | 유모차 (`stroller`) | `category_id` |
| 기본 정보 | 옵션 ID | SYN-STROLLER-001-GREY | `variant_id` |
| 기본 정보 | 시장 구분 | 한국 데모 (`KR_DEMO`) | `market` |
| 사용 조건 | 사용 모드 | 좌석 (`seat`) | `eligibility[0].mode` |
| 사용 조건 | 최소 월령 | 6개월 | `eligibility[0].min_age_months` |
| 사용 조건 | 최대 체중 | 22 kg | `eligibility[0].max_weight_kg` |
| 사용 조건 | 필수 발달 조건 | 혼자 앉기 가능 | `eligibility[0].requires` |
| 사용 조건 | 사용 중단 조건 | 체중 22 kg 초과 | `eligibility[0].stop_when_any` |
| 제품 사양 | 유형 | 휴대형 (`compact`) | `specs.type` |
| 제품 사양 | 좌석 장착 무게 | 6.5 kg | `specs.assembled_weight_kg` |
| 제품 사양 | 접힌 크기 | 45 × 25 × 55 cm | `specs.folded_cm` |
| 제품 사양 | 펼친 크기 | 55 × 85 × 105 cm | `specs.unfolded_cm` |
| 제품 사양 | 좌석 최대 하중 | 22 kg | `specs.seat_max_kg` |
| 제품 사양 | 바구니 최대 하중 | 3 kg | `specs.basket_max_kg` |
| 제품 사양 | 신생아 사용 구성 | 지원 안 함 | `specs.newborn_setup` |
| 편의 기능 | 한 손 접기 | 가능 | `specs.one_hand_fold` |
| 편의 기능 | 접힌 상태 자립 | 불가능 | `specs.self_standing_folded` |
| 구성·호환 | 호환 어댑터 목록 | 등록 항목 없음 (`[]`) | `specs.compatible_adapter_ids` |
| 구성·호환 | 구성품 목록 | 등록 항목 없음 (`[]`) | `components` |
| 판매 조건 | 판매가 | 290,000원 | `offer.price_krw` |
| 판매 조건 | 배송비 | 3,000원 | `offer.shipping_krw` |
| 판매 조건 | 재고 | 10개 | `offer.stock` |
| 판매 조건 | 리드타임 | 3일 — 출고/도착 기준은 원본에 미정의 | `offer.lead_time_days` |
| 데이터 상태 | 가상 데이터 여부 | 예 (`true`) | `is_synthetic` |
| 데이터 상태 | 스펙 값 상태 | `specs`의 10개 최상위 항목 모두 `known` | `field_status` |
| 데이터 상태 | 시뮬레이션 안전 평가 | 미평가 (`not_evaluated`) | `simulated_safety_status` |
| 관리 정보 | 데이터 버전 | 1.0 | `dataset_meta.version` |
| 관리 정보 | 난수 시드 | 42 | `dataset_meta.seed` |
| 관리 정보 | 시나리오 ID | SYN-CASE-001 | `dataset_meta.scenario_id` |

`known`은 가상 시나리오에서 값이 정해져 있다는 의미이며 실제 제품 검증 완료를 의미하지 않는다. 구성품의 빈 목록을 제품 본체가 없다는 뜻으로 해석하지 않는다. 옵션 ID의 GREY 외에 별도 색상 속성은 원본에 없다.

| 파생 항목 | 계산 | 값 |
|---|---|---|
| 1개 구매 시 상품·배송 합계 | 290,000원 + 3,000원 | 293,000원 |

합계는 원본의 판매가와 배송비만 합산한 파생값이다. 원본 JSON은 변경하지 않았다.
