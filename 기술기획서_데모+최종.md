# Plan Basket 기술 기획서 (데모 + 최종)

> 팀 회의 공유용 초안. 회의 후 최종 기획서 틀로 이관 예정.
> 대상 도메인(데모): **컴퓨터(PC 본체 조립) / 유아용품** 2개. (요리는 데모 제외, 최종 후보)
> UI 언어: 한국어. 참고: mindtrip.ai(3패널 chat+canvas), PCPartPicker(슬롯 리스트).
> 최종 수정: 2026-09-10 — 로그인·권한 / 챗봇 대화 주도 전환 / 데이터 소스(네이버 종료→합성) / **[3-C] 적대적 디베이트 → 근거 검증으로 교체** / **리뷰 클렌징 설계 반영(§13)**

---

## 0. 문서 개요

### 0-1. 서비스 한 줄 정의
목적을 말하면(예: "롤·옵치 150만원") AI가 **검증된 근거와 함께 장바구니 전체를 완성**해 주는 쇼핑 플래너. 결제는 외부 쇼핑몰에서.

### 0-2. "데모" vs "최종"의 의미
| | 데모 (해커톤) | 최종 (프로덕션) |
|---|---|---|
| 목표 | 전체 흐름이 실제로 도는 것을 시연 | 트래픽·데이터·운영을 견디는 서비스 |
| 데이터 | **전부 합성/큐레이션** (§16) | 자동 수집 + 제휴 API + 크라우드 |
| 인프라 | EC2 1대 + Docker Compose | ECS Fargate + RDS + CloudFront 등 |
| 학습 루프 | 로깅 스키마만 | 배치 가동 |
| [3-C] 검증 | **시나리오별 정답값 주입** (§10-9) | 실제 도구·RAG 연동 + 리뷰 클렌징 배치(§13) |

### 0-3. 엔진 6단계 (+ 화면 진입 + 로그인 + RAG + 되먹임)
```
화면 진입(S1~S3) → [1] 의도분해·슬롯필링 → [2] 요구사양 빌드
→ [3-0] 후보 수집 → [3-A] 하드 필터 → [3-B] 적합도·병목 순위
→ (분기)
   컴퓨터:  [4] 세트 최적화 → [3-C] 세트 검증  ─(신뢰도<80)⟳
   유아:    [3-C] 품목별 검증 → [4] 예산 배분  ─(신뢰도<80)⟳
→ [5] 설명 생성 → 화면 결과(S4~S5-b, S5-a에서 로그인 게이트)
[6] 사후 학습 = 별도 오프라인 배치
RAG = 공통 컴포넌트, 소비처 2곳([3-C] 검증 근거 · [5] 정당화 근거)
리뷰 클렌징 = 별도 오프라인 배치 → review_summaries → [3-B]·[3-C] 소비
```

### 0-4. 카테고리별 검증 순서가 다른 이유
- **컴퓨터**: 부품이 서로 강하게 상호작용(소켓·전력·물리) → 먼저 세트를 만들고(`[4]`) 완성 조합을 통째로 검증(`[3-C]`).
- **유아**: 품목 간 상호작용 약함 → 품목마다 먼저 검증(`[3-C]`)하고 통과분으로 예산 배분(`[4]`).

### 0-5. 담당 경계
| 영역 | 담당 |
|---|---|
| 엔진 [1]~[5] 로직·스키마·오케스트레이션 / 로그인·권한 / 인프라 | 본 문서 |
| **리뷰 진위 탐지 · 오프라인 리뷰 클렌징 · `review_summaries` 산출** | 리뷰 담당 팀원 — 탐지 설계·실측·데이터 계획은 **§13**, `[3-C]` 인터페이스 계약은 **§10-6** |
| **유아용품 상품 카탈로그** | 유아 담당 팀원 (작성 중) — 완성 시 전달 |
| 컴퓨터 `parts_catalog` 스펙·perf_tier | 본 문서 초안 → 팀 검토 |

---

## 1. 확정 사항 요약 (컴포넌트별 잠금 결정)

| 컴포넌트 | 확정 |
|---|---|
| 공통 | 카테고리 2개(컴퓨터/유아). Strands는 `[3-C]` **도구 오케스트레이션**에만 사용. 그 외는 순수 Converse |
| 화면/[1] | 카테고리 필수 선택 후 자유입력. **되묻기 = AI 대화가 주도, 선택지는 말풍선 안 인라인 칩**(강제 관문 아님). 파싱 실패 시 명시 카드로 fallback. 카드/칩 그룹별 `single\|multi` |
| 화면/[1] | `[추천 실행]`은 `required_inputs` 충족 시 활성. 미응답 조건은 동적 기본값 |
| [1] | 챗봇은 순수 `bedrock-runtime.converse()` + tool schema. Strands 미사용 |
| [1] | **동적 기본값** — 첫 발화(목적어) 기반으로 LLM이 미입력 슬롯 기본값 제안. yaml `defaults:`는 fallback |
| [1] | 컴퓨터 **신규 견적 + 업그레이드** 2모드 (모드 카드로 분기) |
| [1] | 데모: 컴퓨터 **본체 8슬롯만**. 모니터·주변기기 질문 카드 숨김(yaml 주석 보존) |
| 로그인 | 이메일 6자리 코드 → JWT. 소셜 없음. **비로그인도 대화·추천·근거 전부 사용.** 저장·확정·알림·리뷰만 로그인 (§3) |
| [2] | `perf_tier` = 이산 **1~10** 수기 (GPU 상대 서열 / CPU 코어·클럭). 최종은 연속 0~100 벤치 정규화 |
| [2] | 데모 게임 8개: 롤·오버워치·발로란트·배그·FC24·사이버펑크2077·GTA5·엘든링 |
| [2] | PSU 헤드룸 계수 K=1.5 고정(데모). 미등록 게임 = 표준 AAA 프로필(데모) |
| [2] | 예산 배분: `[2]`는 가이드라인만, `[4]`가 authoritative |
| [2] | 업그레이드 데모 = "그래픽카드만 교체" 1시나리오. 연쇄 교체는 경고만 |
| [3-0] | **네이버쇼핑 API 종료 → 데모 후보·가격은 전부 합성 카탈로그**(§16). 후보 수집을 별도 스텝으로 분리 |
| [3-A] | Pending 감점 = Pass 대비 스코어 **−20%**, Pass 부족 시만 top-N 승격 |
| [3-A] | 데모 유아 리콜/KC = 큐레이션 룩업. 최종 = 공공 API |
| [3-B] | top-N: GPU·CPU **5**, 나머지 **3** |
| [3-B] | `balance_profiles` 5행 초안 채택 (GPU-bound 가정). **리뷰 축은 리뷰 담당 팀원 산출물 소비** |
| [4] | 컴퓨터 = 완전탐색(≈27k) + 가지치기. underspend 약한 페널티 + "상향 가능" 제안 |
| [4] | 재탐색 hardcap **3라운드**. 유아 `default_qty` 수기 |
| [4] | 업그레이드 연쇄 교체분은 예산 미포함, "추가 필요" 별도 표시 |
| [3-C] | **적대적 디베이트 폐기.** 도구 관측값 + RAG 근거를 **규칙(judge)으로 채점**. LLM(Haiku)은 쟁점 문장화 1회만 |
| [3-C] | **리뷰 진위는 판정하지 않고 `review_summaries` 사전계산 값을 읽기만** 한다 (산출 로직 §13) |
| [3-C] | `cleanse_ratio` 임계 **20%**. 감점 배점표 초안 채택 |
| [3-C] | 유아 리콜 히트 → 배지 즉시 + 검증 진행. 재탐색 3회 초과 → 안전축 미해결이면 제외 |
| [3-C] | **데모 = 시나리오별 정답값 주입** (tool 고정 반환 + 미니 코퍼스 + confidence 시드). 쟁점 문장만 Bedrock 생성. **실제 탐지 성능 아님** |
| [5] | 기여도 **3축 고정**(가격/성능/호환성). 리뷰는 카드에 평점 별도 |
| [5] | 데모 = breakdown 집계, 최종 = perturbation 옵션. LLM 1회 호출 + 규칙 템플릿 fallback |
| [6] | 데모 = `feedback_events` 스키마+로깅만. 최종 = 주 1회 배치. 승인 게이트 |

---

## 2. 화면 진입 · 조건 수집 (S1 ~ [추천 실행])

목업 대응: A1(랜딩) → A2(카테고리 선택) → A3(조건 수집).

### 2-1. 화면·상태
| 화면 | 내용 |
|---|---|
| S1 랜딩 (A1) | 고정 히어로 + "이렇게 동작합니다" 3스텝. 상단 우측 "리뷰 둘러보기"·"로그인". CTA "채팅 시작하기 · 가입 없이 바로 사용". **리스트 이력 없음** |
| 사이드바 | "+ 새 대화" + "대화 기록"(리스트 이력, 비로그인도 `browser_token`으로 표시) + "리뷰" · "로그인" |
| S2 카테고리 선택 (A2) | 채팅 첫 화면 = 인사말 + 카테고리 카드(각 한 줄 설명). 컴퓨터 선택 시 **모드 카드**([새로 맞추기]/[업그레이드]) 1개 추가. 하단 상시 입력창. 우패널 "물품 리스트 · 비어있음" |
| 슬롯 구조 생성 | 카테고리·모드 확정 즉시 우패널에 전용 슬롯 전부 "아직 선택되지 않음" 렌더 (컴퓨터 8슬롯) |
| S3 조건 수집 (A3) | **대화가 주도.** 자유 입력 → AI가 자연어로 후속 질문 + **말풍선 안 인라인 칩**으로 선택지 제시. 사용자는 칩을 누르거나 / 무시하고 타이핑하거나 / 한 문장에 여러 슬롯 답변 가능. `[이 조건으로 찾기]` 버튼은 `required_inputs` 충족 시 활성 |

### 2-2. 조건 수집 UX — 카드 관문 → 대화 주도 + 인라인 칩
**결정**: "챗봇 부각" = 선택 카드를 없애는 게 아니라 **AI 말풍선 안으로 넣고 강제 관문에서 입력 보조로 강등**.

| | 방식 |
|---|---|
| 칩 클릭 | **서버 직접 처리(결정적, 빠름)** — LLM 호출 없음. `question_sets`의 `maps_to` + 내부 매핑 규칙 |
| 자유 답변 | `[1]` 슬롯필링 LLM 호출. 다중 슬롯 답변 파싱 ("qhd로 하고 조립은 대행이요") |
| **파싱 실패** | "그 부분은 이 중에서 골라주세요" + **그때 명시적 카드 제시** → 카드가 안전망으로 남음 |
| 진행 투명성 | 우패널 슬롯 + "남은 조건 N개" 미니 표시 |

- `question_sets` yaml **그대로 유지** — 인라인 칩 소스 + 파싱 실패 fallback UI로 재사용
- 순수 챗(칩 완전 제거)은 **비채택** — 콜드스타트·파싱 실패율·데모 예측성 손해

### 2-3. 데이터
```sql
lists (
  id uuid pk, user_id null, browser_token text,
  category text,              -- 'computer' | 'baby'
  mode text,                  -- computer: 'build'|'upgrade'
  status text,                -- collecting | ready | running | done
  objective_text text,        -- 첫 발화 원문 (동적 기본값용)
  slots jsonb,
  created_at, updated_at
)
messages (id, list_id fk, role, text, ts)
```
- 대화 1건 = 리스트 1건. 비로그인은 `browser_token`, 로그인 시 `user_id` 연결.
- `lists` 행 생성: "채팅 시작하기" 클릭 시.

### 2-4. API
| 메서드 | 경로 | 동작 |
|---|---|---|
| POST | `/session` | 리스트 생성 → `{list_id, browser_token}` |
| POST | `/session/{id}/category` | `{category, mode?}` → 슬롯 구조 + 필수입력 + 질문세트 |
| POST | `/session/{id}/message` | `{text}` → LLM 슬롯필링 → `{slots, assumed, missing, next_questions, can_recommend}` |
| POST | `/session/{id}/answer` | `{question_id, selected[]}` → 서버 직접 갱신 (LLM 0회) |
| PATCH | `/session/{id}/slot` | `{field, value\|null}` → 수정·삭제 |
| POST | `/session/{id}/recommend` | 엔진 파이프라인 트리거 |

- 카테고리 미선택 상태에서 `/message` → `400`.

### 2-5. 미결정 (회의 안건)
- 카테고리 중간 변경 시 기존 `slots` 처리
- 도메인 밖 입력 가드 ("김치찌개 레시피")
- messages 저장 — 별도 테이블 확정

---

## 3. 로그인 · 권한 (인증·인가)

### 3-1. 설계 원칙
비로그인도 **핵심 가치(대화·추천·근거)는 전부** 사용. 로그인은 "내 것으로 저장·추적·기여"할 때만.

### 3-2. 비로그인 허용 vs 로그인 필요
| 액션 | 비로그인 | 로그인 |
|---|---|---|
| 카테고리·대화·조건 수집·추천 실행·결과·추론 로그·리뷰 상세·재추천 | ✅ | ✅ |
| 대화(리스트) 임시 보관 | ✅ `browser_token` (기기·브라우저 한정) | — |
| **리스트 확정** (이름·구매예정일·목표가 저장) | ❌ | ✅ |
| **목표가 이메일 알림** | ❌ | ✅ |
| **리뷰 작성·구매 인증·개봉 확인** | ❌ | ✅ |
| 여러 기기에서 내 리스트 이어보기 / 패널 설정 저장 | ❌ | ✅ |

**로그인 트리거**: S5-a "리스트 확정" 버튼 → 비로그인이면 로그인 모달. 그 외엔 유도 배너 정도, 강제 안 함.

### 3-3. 인증 방식 — 이메일 6자리 코드 (소셜 없음)
```
1. 이메일 입력 → POST /auth/request-code {email}
2. 서버: 6자리 코드 생성 → auth_codes(email, code_hash, expires_at, attempts, consumed) 저장(해시) → SES 발송
3. 코드 입력 → POST /auth/verify {email, code}
4. 서버: 해시·만료·시도 확인 → users upsert(처음이면 계정 생성) → JWT 발급
5. 클라이언트: JWT httpOnly 쿠키 보관
6. browser_token 병합: 비로그인 lists(browser_token 소유) → user_id로 이관
```

| 항목 | 값 |
|---|---|
| 코드 | 6자리 숫자, 유효 10분, 1회용(`consumed`), 해시 저장(argon2/bcrypt) |
| 검증 시도 한도 | 5회 초과 시 코드 무효 → 재요청 |
| 코드 재요청 rate limit | 이메일당 60초 간격, 시간당 5회 |
| 이메일 존재 노출 방지 | 요청 응답은 항상 "코드를 보냈습니다" (enumeration 차단) |
| JWT | 데모: access 2주 단일 / 최종: access 1h + refresh 30d + 폐기 목록. 클레임 `sub, email, iat, exp` |

### 3-4. DB
```sql
users (id uuid pk, email text unique, created_at, last_login_at, display_name null)
auth_codes (id, email, code_hash, expires_at, attempts int default 0,
            consumed bool default false, created_at, INDEX(email, created_at))
```

### 3-5. API
| 메서드 | 경로 | 동작 |
|---|---|---|
| POST | `/auth/request-code` | `{email}` → 코드 발송 (항상 200) |
| POST | `/auth/verify` | `{email, code}` → JWT + browser_token 병합 |
| POST | `/auth/logout` | 토큰 폐기 |
| GET | `/auth/me` | 현재 사용자 |

### 3-6. 인가 (endpoint 가드)
- `/session/*` (대화·추천) → 인증 불요, `browser_token` 또는 JWT 허용
- `/lists/{id}/confirm`, `/lists/{id}/alert`, `/reviews/*` → **JWT 필수**
- 소유권: `list.user_id == jwt.sub` OR (`list.user_id is null AND list.browser_token == req.browser_token`)

### 3-7. 데모 vs 최종
| | 데모 | 최종 |
|---|---|---|
| 코드 발송 | SES 샌드박스(시연 계정 사전 인증) + 콘솔 로그 fallback | SES production + DKIM/SPF |
| rate limit | 인메모리 카운터 | Redis/DB |
| 세션 폐기 | 쿠키 삭제 | refresh 폐기 목록 |

---

## 4. 엔진 [1] 의도 분해 · 슬롯필링

### 4-1. 책임
자유 대화 + 인라인 칩으로 "추천에 필요한 조건 세트"를 완성. LLM 역할 = (a) 자유 텍스트 → 슬롯 추출 (b) 정의에 없는 조건 자유 추가. 칩 클릭 → 슬롯은 서버가 직접.

### 4-2. 카테고리 정의 파일
`config/categories/computer.yaml`, `baby.yaml` (코드 포함, 부팅 시 로드).
```yaml
category: computer
required_inputs: [purpose, budget_max]
slot_schema:            # LLM structured output tool schema로 그대로 사용
  type: object
  properties:
    purpose:     { enum: [game, creation, office] }
    budget_max:  { type: [integer, "null"] }
    games:       { type: array, items: { type: string } }
    resolution:  { enum: ["FHD_144","QHD_165","4K", null] }   # 해상도+주사율 단일 필드
    assembly:    { enum: [self, service, null] }
    noise_sensitive: { type: [boolean, "null"] }
    brand_pref:  { enum: [intel, amd, none, null] }
    owned:       { type: array, items: { type: string } }
    extra:       { type: array, items: { type: string } }
    # 데모 숨김(주석): monitor_owned, peripherals_included, case_size, expandability
question_sets:
  - { id: q_resolution, select: single, label: "목표 해상도·주사율?",
      options: ["FHD 144Hz","QHD 165Hz","4K","잘 모르겠어요"], maps_to: resolution }
  - { id: q_games, select: multi, label: "어떤 게임?", ... }
  # 조립 / 브랜드 / 소음
slot_structure: [CPU, GPU, RAM, 메인보드, 저장장치, 파워, 케이스, 쿨러]
defaults:               # 동적 기본값 실패 시 fallback
  resolution: FHD_144
  assembly: service
  noise_sensitive: false
  brand_pref: none
```
`baby.yaml` = 같은 구조. `required_inputs: [age_month_or_due_date, budget_range]`.

### 4-3. LLM 호출 (슬롯필링)
자유 텍스트가 올 때만 호출 (칩 클릭은 서버 직접):
```python
resp = bedrock.converse(
  modelId=HAIKU, system=[{"text": SLOT_SYSTEM}],
  messages=[{"role":"user","content":[{"text": f"현재 슬롯:{slots}\n목적:{objective_text}\n사용자:{text}"}]}],
  toolConfig={"tools":[{"toolSpec":{"name":"fill_slots","inputSchema":{"json": SLOT_SCHEMA}}}],
              "toolChoice":{"tool":{"name":"fill_slots"}}}
)
```
`fill_slots` 출력:
```jsonc
{ confirmed: {...}, assumed: {...}, assumed_reason: { 슬롯: "한 줄 근거" }, missing: [...] }
```
- 시스템 프롬프트: "명시 안 한 슬롯은 목적에 맞춰 기본값 제안 → `assumed` + 한 줄 근거. `required_inputs`는 절대 assumed 금지 → `missing`으로."
- 머지: `confirmed` 우선 > `assumed` > yaml `defaults`. `extra`/`owned`는 append(중복 제거).
- 파싱 실패 → 1회 재시도 → 슬롯 유지 + 명시 카드 fallback.
- 비용: 대화 주도 전환으로 세션당 자유입력 4~7회 × Haiku 소형 ≈ $0.02 미만. 동적 기본값은 이 호출에 포함.

### 4-4. 되묻기 루프
```
자유입력 → 슬롯필링 → missing 계산
missing ∩ required 있음 → 그 필드 매핑 칩을 AI 말풍선에 첨부
missing 중 optional만 → 칩 첨부 + "건너뛰기" 허용
missing 없음 → can_recommend = true
"잘 모르겠어요" 선택 → 해당 슬롯 assumed 유지, missing에서 제거
```

### 4-5. 업그레이드 모드
"컴퓨터" 선택 직후 모드 카드. 업그레이드 시:
| | 내용 |
|---|---|
| 필수 입력 | ① 현재 구성(최소 CPU·메인보드·파워) ② 교체 희망 슬롯 ③ 예산 |
| 파싱 | 별도 tool `parse_current_build` — 자유텍스트 → 현재 구성 dict. 부품 DB fuzzy 매칭, 실패 시 되묻기 |
| 되묻기 우선순위 | 파워 용량 · 메인보드 모델 · 케이스 |
| 우패널 | "현재 구성" 패널 + "교체 후보" 슬롯 |

### 4-6. 데모 vs 최종
| | 데모 | 최종 |
|---|---|---|
| 기본값 | 동적(첫 발화 기반), yaml fallback | 동일 + `defaults` 프로필 학습 |
| extra 파싱 | 생략 (표시만) | LLM 분류(하드제약/soft/미반영) |
| 주변기기·모니터·케이스 크기 | 숨김 | 활성 |

---

## 5. 엔진 [2] 요구사양 빌드

슬롯(사용자 언어) → 기계 판정 가능한 목표사양. 100% 규칙/룩업. LLM은 2곳(extra 파싱, 업그레이드 현재구성 파싱)만.

### 5-1. 입출력
- 입력: `[1]` confirmed slots + assumed
- 출력: `RequirementSpec` (DB `requirement_specs`, jsonb)
- 진입 가드: `required_inputs` 미충족 시 `[1]`로 반환

### 5-2. 컴퓨터 — 신규(build)

**게임 → tier 룩업** `game_requirements`:
```
game_key, game_name_ko, resolution('FHD'|'QHD'|'4K'), fps_band('60'|'120+'),
gpu_tier_min, cpu_tier_min, ram_gb_min, vram_gb_min, storage_gb, source_url, collected_at
PK (game_key, resolution, fps_band)
```
| | 데모 | 최종 |
|---|---|---|
| 규모 | 8게임 × 3해상도 × 2밴드 = 48행 수기 | 100+ 게임, 반자동 수집 + 갱신 배치 |
| 다중 게임 | 각 필드 `MAX()` | 동일 |
| 미등록 게임 | 표준 AAA 프로필 + `추정` 플래그 | RAG(벤치 노트) 조회 |

**perf_tier 사다리**
| | 데모 | 최종 |
|---|---|---|
| 형태 | 이산 1~10 수기 (`parts_catalog.perf_tier`) | 연속 0~100 벤치 정규화 |
| 지표 | GPU 상대 서열 / CPU 코어·클럭 | 3DMark Time Spy / Cinebench·PassMark + 자체측정 + 크라우드 |
| 비교 | `part.tier >= req.tier_min` | 동일 |

**PSU 헤드룸 공식**
```
base_w = cpu_tdp + gpu_tgp + 표준부하(≈+75)
required_w = base_w × K            # 데모 K=1.5 고정 / 최종 GPU 세대별
wattage_min = ceil_to_standard(required_w)  # [550,650,750,850,1000,1200]
plus_rating_min = "Gold"
```

**슬롯 조건 → 하드 제약**
| 조건 | 대상 | 제약 |
|---|---|---|
| purpose:game + games | GPU·CPU·RAM | game_requirements floor |
| resolution | GPU·RAM | 위 룩업 포함 |
| brand_pref | CPU·메인보드 | `socket_family in [...]` |
| budget_max | 전체 | budget_alloc 슬롯별 상한(soft) |
| assembly | — | 조립 난이도 태그 → [3-B] soft |
| noise_sensitive | 쿨러·케이스·파워 | 하드 아님 → [3-B] soft |

**link_rules** ([2]는 기록만, [3-A]/[4]가 강제)
```
cpu.socket == mainboard.socket
ram.type   == mainboard.mem_type
gpu.length_mm    <= case.max_gpu_len_mm
cooler.height_mm <= case.max_cooler_height_mm
cooler.tdp_capacity_w >= cpu.tdp
Σ(component power) <= psu.wattage × 0.9
cpu ∈ mainboard 지원 (데모: chipset 근사 bios_compat.yaml ~7규칙 / 최종: 메인보드별 CPU 지원 리스트)
```

**budget_alloc** `budget_profiles(purpose, resolution, alloc jsonb)` — 데모 ~6 프로필 수기, 최종 [6] 학습.

**feasibility 예비 판정**: `est_total = Σ(목표 tier 시장 최저가 추정)` → `<budget×0.9` ok / `~1.1` tight / `>1.1` infeasible.

**출력(build)**:
```jsonc
spec.targets = {
  CPU:{perf_tier_min:6, socket_in:["LGA1851","AM5"], tdp_budget_w:125},
  GPU:{perf_tier_min:7, vram_gb_min:12, tgp_budget_w:300},
  RAM:{type:"DDR5", capacity_gb_min:16, kit:"2x8"},
  메인보드:{socket_in:[...], form_in:["ATX","mATX"], mem_type:"DDR5", chipset_tier_min:"mid"},
  저장장치:{interface:"NVMe", capacity_gb_min:1000},
  파워:{wattage_min:750, plus_rating_min:"Gold"},
  케이스:{form:"ATX_mid"},          // 데모 고정
  쿨러:{tdp_capacity_w_min:125}
}
```

### 5-3. 컴퓨터 — 업그레이드(upgrade)
- `parse_current_build`(LLM, 모드 진입 시 1회): 자유텍스트 → `current = {slot: {raw, matched_id?, specs}}`
- 역방향 제약:
```jsonc
targets[slot] = {
  perf_tier_min: <목표 or 현재 대비 +N>,
  hard_from_current: ["socket == current.메인보드.socket",
                     "mem_type == current.메인보드.mem_type",
                     "length_mm <= current.케이스.max_gpu_len",
                     "tgp_w <= current.파워.wattage×0.9 − 현재_나머지_소비"],
  unknown: ["current.케이스 미입력 → 길이 제약 검증 불가"]
}
```
- 연쇄 교체 경고(데모 = 경고만): GPU 후 Σpower 초과 → "750W 이상 필요" / socket 불일치 CPU → "메인보드도 교체 필요" + "신규 모드 권장" / cpu.tdp > 쿨러 → "쿨러 교체 권장". → `spec.chain_warnings[]`

### 5-4. 유아용품

**월령 계산**: `current_month = floor((today − birth_date)/30.44)`. due_date만 → `mode="prenatal"`.

`age_fit_table(category, sub_item, min_month, max_month, milestone_gate, default_needed, default_qty)`

**시점 축**:
```
prenatal → 지금필요를 "D−" + days_to_due
current ∈ [min,max] → milestone 있으면 owned/월령 근사(불확실 → Pending) / 아니면 "now"
current < min → "D+" + (min−current)×30
current > max → needed=false, 회색
```

**슬롯 조건 → 요구조건**
| 슬롯 | 요구조건 |
|---|---|
| feeding(모유/혼합/분유) | 수유 sub_items 결정 |
| sleep(침대/바닥) | 수면 sub_items |
| car(있음) | 외출에 카시트 needed |
| outing_freq | 유모차 등급 |
| season | 계절 용품 |
| owned[] (multi) | `excluded_by_owned` — [3-A]에서 제외 |
| 전 품목 | `kc_required=true`, `recall_excluded=true` |

**출력(baby)**:
```jsonc
spec = {
  age_context:{current_month:6, source:"birth_date 2026-03-10", mode:"born"},
  categories:{
    수유:{needed:true, timing:"now", sub_items:["젖병","젖병소독기","분유포트"],
         constraints:{age_fit:[0,12], kc:true, recall_excl:true}, reason_from_slot:"혼합수유"},
    수면:{...}, "위생/기저귀":{...},
    외출:{needed:true, timing:"D+30", sub_items:["아기띠"], constraints:{milestone:"목가누기", age_fit:[3,15]}}
  },
  excluded_by_owned:["아기침대","바운서"],
  budget_total:500000, budget_alloc:{수유:0.35, 수면:0.25, "위생/기저귀":0.25, 외출:0.15}
}
```

### 5-5. RequirementSpec 공통 래퍼
```jsonc
RequirementSpec = {
  list_id, category, mode,
  source_slots, assumed, assumed_reason,
  spec: <build | upgrade | baby>,
  link_rules: [...],            // computer만
  chain_warnings: [...],        // upgrade만
  budget: { total, alloc, feasibility },
  flags: ["4K60_스케일링_추정", "미반영: 색상 통일", ...],
  unresolved: [ {slot, reason} ],
  built_at
}
```

### 5-6. 엣지 케이스
| # | 상황 | 처리 |
|---|---|---|
| E1 | 예산 < 목표 최소가 | `feasibility="infeasible"` → 출력 후 [4] 완화안 |
| E2 | 다중 게임 편차 큼 | 각 필드 MAX = 최고사양 게임 기준 |
| E3 | 미등록 게임 | 데모: 표준 프로필+`추정` / 최종: RAG |
| E4 | 브랜드 고정 + 저예산 | 제약 유지 → [3-A] 희소 → 재탐색/완화 |
| E5 | 전 슬롯 "잘 모르겠어요" | assumed 전부로 생성, `tight` 경고 |
| E6 | 업그레이드 케이스·파워 미입력 | `unresolved` 등록 → 되묻기 |
| E7 | 업그레이드 socket 불일치 CPU | `chain_warnings` + "신규 모드 권장" |
| E8 | 유아 due_date만 | `mode="prenatal"`, D− 기준 |
| E9 | 유아 월령 지난 품목 | `needed=false`, 회색 |
| E10 | 유아 milestone 불확실 | Pending + 되묻기 |
| E11 | extra 반영 불가 | `flags`에 "미반영: <원문>" + S4 고지 |
| E12 | [1] required 미충족 | 진입 거부 → [1] 반환 |

---

## 6. 엔진 [3-0] 후보 수집

`[2]`와 `[3-A]` 사이 별도 스텝. **데모는 네이버쇼핑 API 종료로 라이브 호출 없음 — 로컬 합성 카탈로그만 읽음**(§16). 재탐색 시 스킵.

| | 컴퓨터 | 유아용품 |
|---|---|---|
| 후보 소스 (데모) | `parts_catalog` (합성, §16) | `baby_products` (합성, 유아 담당 팀원) |
| 후보 소스 (최종) | parts DB(스펙) ⨝ 제휴 API(가격·판매처·재고) | 제휴 API 검색 ⨝ 공공데이터포털 리콜 API ⨝ KC 인증 API |
| 공통 | 제품 단위 dedupe(최저가 1개). 데모는 카탈로그에 이미 포함 | 동일 |

출력: `slots: { GPU:[candidate...], ... }` (판정 전).

**데모 부수효과**: SSM에 네이버 키 불필요, 레이트리밋·캐시 TTL·네트워크 실패 처리 데모 경로에서 제거.

---

## 7. 엔진 [3-A] 하드 필터 (Pass / Fail / Pending)

규칙으로 확정 판정 가능한 것만. LLM·RAG 없음.

### 7-1. verdict 정의
| verdict | 의미 | 처리 |
|---|---|---|
| Pass | 모든 하드 조건 충족 확인 | [3-B]로 |
| Fail | 하드 조건 위반 확인 | 후보 제외 (개수만 로그) |
| Pending | 판정 데이터 없음 (스펙 누락 등) | 후보 유지, [3-B] 감점, [3-C] 우선 검증 |

**Pending ≠ Fail.**

### 7-2. 컴퓨터 규칙
슬롯 내부 조건 (`RequirementSpec.targets[slot]`):
| 슬롯 | 조건 |
|---|---|
| CPU | `socket ∈ socket_in` · `perf_tier ≥ min` · `tdp ≤ tdp_budget_w` |
| GPU | `perf_tier ≥ min` · `vram_gb ≥ min` · `length_mm ≤ len_max`(케이스 확정 시) · `tgp_w ≤ budget` |
| RAM | `type == target.type` · `capacity_gb ≥ min` |
| 메인보드 | `socket ∈ socket_in` · `form ∈ form_in` · `mem_type == target.mem_type` · `chipset_tier ≥ min` |
| 저장장치 | `interface == NVMe` · `capacity_gb ≥ min` |
| 파워 | `wattage ≥ wattage_min` · `plus_rating ≥ Gold` |
| 케이스 | (데모 고정) `form == ATX_mid` |
| 쿨러 | `tdp_capacity_w ≥ min` |

- **단방향 확정 제약**만 [3-A]에서. **양방향 조합 제약**(`link_rules`)은 [4]/[3-C]로.
- BIOS — 데모: chipset 근사 → "AM5 600칩셋 + Ryzen 9000" → **Pending** + "BIOS 업데이트 필요". 최종: 미지원 → Fail.

업그레이드: 교체 후보 슬롯만. `hard_from_current` 위반 → Fail. `unknown` → Pending.

### 7-3. 유아 규칙
| 조건 | Pass | Fail | Pending |
|---|---|---|---|
| 월령 적합 | 범위 겹침 | 벗어남 | 제품 월령 정보 없음 |
| KC 인증 | 확인됨 | 만료·취소 | 정보 미확인 |
| 리콜 이력 | 매칭 없음 | 매칭 → **배지 최우선** | 브랜드만 부분 매칭 |
| excluded_by_owned | — | `owned` 매칭 시 후보 생성 안 함 | — |

### 7-4. reason codes
```
PASS / FAIL_SOCKET_MISMATCH / FAIL_PERF_BELOW / FAIL_VRAM_BELOW / FAIL_PSU_UNDER
FAIL_RECALL / FAIL_KC_REVOKED / FAIL_AGE_OUT_OF_RANGE / FAIL_OWNED_EXCLUDED
PENDING_SPEC_MISSING:<field> / PENDING_KC_UNVERIFIED / PENDING_BIOS_UPDATE / PENDING_RECALL_MAYBE
```

### 7-5. 출력
```jsonc
HardFilterResult.slots.GPU = {
  pool_size: 214, pass_count: 18, fail_count: 190, pending_count: 6,
  candidates: [ { product_id, name, price, verdict, reasons:[...], specs:{...}, flags:["tier_추정"] }, ... ]
}
```

### 7-6. 슬롯별 후보 0
| 상황 | 처리 |
|---|---|
| Pass 0, Pending>0 | Pending을 후보로 진행 + "미검증만" 경고 |
| Pass 0, Pending 0 | 하드 완화 트리거 → [2] 피드백(tier↓/해상도↓) 또는 "예산 N만원 올리면" |

---

## 8. 엔진 [3-B] 적합도 · 병목 순위

[3-A] 통과분을 슬롯별 점수화 → 슬롯별 상위 N개. LLM·RAG 없음.

### 8-1. 점수 모델
```
score(c) = Σ_axis w_axis · norm_axis(c) − penalty(c)
penalty(c) = 0.20 if verdict == Pending else 0
```

축 (컴퓨터): 가격 `1 − price/slot_budget` / 성능 여유 `perf_tier − tier_min`(balance 초과분 감쇠) / 밸런스 적합 `1 − |tier − ideal_tier|/4` / **리뷰 신뢰도** `cleaned_rating/5 × (1 − cleanse 가중)` — 리뷰 담당 팀원 산출 `review_summaries` 소비 / 호환 여유(파워·길이 마진).

축 (유아): 월령 적합도 / 리뷰 신뢰도 / 가격 / 안전 확실성(KC 확인 1.0 > Pending 0.5) / 시점 우선순위(now 1.0 > D+n 0.6 > later 0.2).

### 8-2. 가중치
```
DEFAULT_WEIGHTS:
  computer: {가격:0.35, 성능:0.25, 밸런스:0.15, 리뷰:0.20, 호환여유:0.05}
  baby:     {월령적합:0.30, 안전확실성:0.25, 리뷰:0.20, 가격:0.15, 시점:0.10}
```
슬롯 기반 조정(재정규화): `noise_sensitive` → 소음 리뷰축 +0.05 / `assembly:self` → 조립 난이도축 +0.05 / `assumed` 슬롯 → 성능축 −0.05, 가격축 +0.05 / 전 슬롯 "잘 모르겠어요" → 가격축 +0.10. → `weight_adjustments[]` 기록.

### 8-3. 병목
`balance_profiles(purpose, resolution) → {gpu_ideal_tier, cpu_ideal_tier, cpu_gpu_offset}`

**데모 초안 (5행, GPU-bound 가정)**:
| purpose | resolution | GPU ideal | CPU ideal | 근거 |
|---|---|---|---|---|
| game | FHD 144Hz | 6 | 6 | 고주사율 = CPU 프레임 한계 지배 |
| game | QHD 165Hz | 7.5 | 5.5 | 균형, GPU 우위 |
| game | 4K | 9 | 5.5 | GPU가 전부 |
| creation | — | 6.5 | 8 | 렌더·인코딩 CPU 코어 수 지배 |
| office | — | 2 | 3 | 내장그래픽 허용 |

`bottleneck_hint`: `budget_alloc 내 최선 후보.tier < ideal_tier − 0.5` → `{slot, gap, suggestion}` 방출 → [4] 예산 재분배 시 우선 반영.

### 8-4. top-N
| 슬롯 | N |
|---|---|
| GPU, CPU | 5 |
| RAM, 메인보드, 파워, 저장장치, 케이스, 쿨러 | 3 |
| 유아 각 sub_item | 3 |

### 8-5. 출력
```jsonc
RankResult.slots.GPU = {
  ideal_tier: 7.5,
  ranked: [ { product_id, name, price, verdict_from_3a, rank, score,
              breakdown:{가격:0.41, 성능:0.19, 밸런스:0.12, 리뷰:0.20, 호환여유:0.05} }, ... ],
  bottleneck_hint: null
}
```
`breakdown` → [5]가 빌드 전체로 집계 → 목업 A5 "가격 41% / 성능 33% / 호환성 26%".

### 8-6. 엣지
| # | 상황 | 처리 |
|---|---|---|
| E1 | Pending만 존재 | 랭크 + `all_unverified` 플래그 |
| E2 | 후보 1개 | rank 1, score 0.5 통과 |
| E3 | 전 후보 동일 가격/tier | 축값 0.5 중립 |
| E4 | `review_summaries` 없음 | 리뷰축 0.5 + 가중 재분배 |
| E7 | Pending이 −20% 후에도 Pass보다 높음 | Pass ≥ N → top-N은 Pass만 / Pass < N → 나머지 top Pending |

---

## 9. 엔진 [4] 세트 최적화 / 예산 배분

### 9-A. 컴퓨터 — 세트 최적화 ([3-C] 앞)

- 입력: `RankResult`(GPU·CPU 5, 나머지 3) + `link_rules` + `budget` + `bottleneck_hints`
- 방법: **완전탐색 + 가지치기** (≈27,000 조합, 데모 <1s). LLM·RAG 없음.
- 출력: `BuildResult` (슬롯당 1개, 완성 세트)

**하드 제약 (위반 조합 가지치기, 컷 순서)**:
```
1. cpu.socket == mainboard.socket      2. ram.type == mainboard.mem_type
3. gpu.length_mm <= case.max_gpu_len   4. cooler.height_mm <= case.max_cooler_height
5. cooler.tdp_capacity_w >= cpu.tdp    6. Σ(power) <= psu.wattage × 0.9
7. total_price <= budget_max           8. BIOS 지원 (데모 chipset 근사, "업데이트 필요"는 컷 아님)
```

**목적함수(최대화)**:
```
build_score = Σ_slot (slot_weight · candidate.score)
            + pairing_bonus(1 − |(gpu_tier − cpu_tier) − ideal_offset|/4)
            + headroom_bonus
            − underspend_penalty (예산 85% 미만 사용 시 감점 + "GPU 한 단계 상향 가능" 병기)
```

**출력**:
```jsonc
BuildResult = {
  list_id,
  items: [ { slot:"GPU", product_id, name, price, perf_tier, score, rank_from_3b }, ... ],  // 8슬롯
  totals: { price, power_w, avg_score },
  budget: { max, used, used_pct, slack },
  link_check: { socket:"ok", power:"ok (420/675)", gpu_len:"ok", cooler_height:"ok", bios:"pending: 업데이트 필요 가능" },
  balance: { gpu_tier:7, cpu_tier:5, ideal_offset:2, delta:0, verdict:"균형" },
  alternatives: { considered:27000, valid:3200 },
  round: 1, optimized_at
}
```

**재탐색 루프**: `[3-C]` 신뢰도 < 80 + 최대 감점 슬롯 → 해당 후보 exclusion set 추가 → 그 슬롯 차선 후보 → **세트 전체 재최적화** → 재검증. **hardcap 3라운드** → 실패 시 best-so-far + "검증 미완료" 표시.

**Infeasible**:
| 상황 | 처리 |
|---|---|
| 예산 내 유효 조합 0 | "+N만원이면 가능" / [2] 피드백(해상도↓·fps↓·게임 축소) / 초과분 강조 최저가 빌드 |
| 예산 무시해도 조합 0 (link 불가) | [3-0] 재수집 or 사용자 완화 요청 |
| BIOS pending 조합만 가능 | 채택 + "메인보드 BIOS 업데이트 필요" 경고 최상단 |

**업그레이드**: 교체 슬롯만 최적화, 나머지 `current` 고정. 연쇄 교체분(파워 등)은 예산 미포함, "추가 필요" 별도 표시.

### 9-B. 유아 — 예산 배분 ([3-C] 뒤)

- 입력: 품목별 검증 통과 후보 + `RequirementSpec.baby`(needed·timing·budget)
- 방법: greedy 1-pass (조합 상호작용 없음)

```
항목 정렬: (timing 우선순위 desc, 카테고리 alloc 적합, [3-B] score desc)
running = budget_total
for item in 정렬:
  if timing == "later": deferred[] 추가, continue
  best = 검증 통과 후보 중 최고 점수
  if running − best.price >= 남은_now_예약분: 확정; running −= best.price
  else: cheaper = 최저가 검증 후보; affordable? 확정+"하향" 플래그 : "예산 초과" 표시
leftover 재분배: 더 좋은 검증 후보가 들어가는 항목 업그레이드
```

**수량**: `age_fit_table.default_qty` (젖병 3, 손수건 10 등). PC 부품은 항상 1.

**출력**:
```jsonc
BasketResult = {
  list_id,
  buy_now: [ { category, sub_item, product_id, name, price, qty, score, timing:"now" }, ... ],
  buy_later: [ { category, sub_item, price_est, timing:"D+30", reason:"목 가누기 이후" } ],
  totals: { now_price, later_price_est },
  budget: { total, now_used, slack },
  adjustments: ["젖병소독기: 예산 맞춰 차선 후보로 하향"],
  allocated_at
}
```

**엣지**: 모든 now 항목 초과 → 필수도(수유>수면>위생>외출) 순으로 자르고 "예산 N만원 더 필요" / later만 남음 → "지금 필요한 건 없어요" + 타임라인 / owned로 카테고리 제외 → alloc 재분배.

### 9-C. 컴퓨터 엣지
| # | 상황 | 처리 |
|---|---|---|
| E1 | 조합 동점 | tie-break: 낮은 총액 → 높은 리뷰 신뢰도 → 높은 밸런스 |
| E2 | budget_alloc 무시가 최적 | alloc은 guide, objective 이기면 허용 + bottleneck_hint 반영 |
| E3 | 재탐색 3회 실패 | best-so-far + "검증 미완료: <항목>" |
| E4 | PSU top-3 전부 전력 부족 | [3-0] 재수집 or GPU 하향 |

---

## 10. 엔진 [3-C] 근거 검증

규칙으로 못 잡는 것(조합 이슈·리뷰 신뢰·자유 근거)을 **도구 조회 + 근거 검색 + 규칙 채점**으로.

> **판정은 LLM이 하지 않는다 (확정 — 리뷰 담당 팀원 탐지 실험 결과 반영)**
> - **LLM에게 "이 리뷰 가짜냐" 직접 판정 금지** (정확도 50%, 틀리면서 확신). 텍스트만으로 판정하면 오탐이 **만족 고객·짧은 리뷰에 집중**(1.8배)되고, 난독화 다섯 겹이면 탐지율이 **0.0%**가 된다.
> - 그래서 `[3-C]`는 **도구가 낸 관측값과 RAG 근거를 규칙(judge)으로 채점**한다. LLM(Haiku)은 쟁점을 사람이 읽는 문장으로 옮기는 데만 쓴다 — **판정이 아니라 표현**.
> - 리뷰 진위는 `review_summaries` **사전계산 라벨을 읽기만** 한다. 산출 로직은 **§13 리뷰 클렌징**.
> - **용도 제한(확정)**: 자동 삭제·소비자 "AI 작성" 라벨 = 배제 / 랭킹 하향 = 단독 사용 금지 / **검토 큐 우선순위·캠페인 모니터링 = 채택**.
> - 임계값은 **도메인별로 재조정 필요** (컴퓨터 vs 유아). 현 `cleanse_ratio` 20%는 공통 초안값.

### 10-1. 입출력
| | 컴퓨터 | 유아용품 |
|---|---|---|
| 순서 | `[4]` 뒤 | `[3-B]` 뒤, `[4]` 앞 |
| 입력 | `BuildResult` (세트 1개) | 슬롯별 `[3-B]` 통과 후보들 |
| 단위 | 세트 전체 1건 | 품목별 독립 |
| 출력 | `VerificationResult` — 신뢰도(0~100)·통과(≥80)·쟁점·회색축·검증 로그 |
| 통과 | → `[5]` | 품목별 통과분 → `[4]` |
| 미달 | → `[4]` 재구성 | → `[3-B]` 다음 후보 → 재검증 |

### 10-2. 아키텍처
```python
def verify(subject, axes):               # 요청마다 fresh — 상태 누수 방지
    findings = [call_tool(ax, subject) for ax in axes]      # 규칙·룩업·사전계산값
    evidence = [evidence_search(domain, ax.query, filters)  # RAG
                for ax in axes]
    score    = judge(findings, evidence)  # 규칙 함수 (LLM 아님) → 신뢰도·감점
    text     = narrate(score.issues)      # Haiku 1회 — 문장화만, 판정 없음
    return score, text
```
`@tool`: `evidence_search(domain, query, filters)` (RAG, 공용) / `check_power_headroom` / `check_bottleneck` / `check_physical_fit` / `check_bios_support` / `lookup_recall` / `lookup_kc_cert` / `check_age_fit` / `get_review_authenticity`.

Strands는 **도구 오케스트레이션에만** 쓴다(축별 호출·병렬화·재시도). 에이전트 간 대화는 없다.

### 10-3. 검증 프로토콜 (2단계)
```
S1 수집: 축별 @tool 호출 → 관측값 (위반 / 정상 / 데이터 없음)
         축별 evidence_search → 근거 스니펫 첨부
S2 채점: judge(규칙) → 축별 배점 감점 → 신뢰도 = 100 − Σ(쟁점 감점)
         근거 0건인 축은 감점 아님 → "검증 불가(회색)"로 남긴다
```
judge 채점:
| 상태 | 처리 |
|---|---|
| 도구 명확히 "위반" | 축별 배점 감점 |
| 도구 "정상" | 감점 없음 |
| 도구 결과 ↔ RAG 근거 상충 | 중간 감점 + 회색 표시 |
| 근거 0건 | 감점 아님 → "검증 불가(회색)" |
| 도구 결과 없음 (Pending) | 감점 아님 → 회색. `[3-B]` 감점으로 이미 반영됨 |

`신뢰도 = 100 − Σ(쟁점 감점)`

**LLM 출력은 채점에 들어가지 않는다.** 문장화 결과가 이상해도 신뢰도는 흔들리지 않는다 — 환각이 점수를 오염시키는 경로 자체를 없앤 것이다.

### 10-4. 컴퓨터 세트 검증 축 + 감점 배점표 초안
| 축 | 도구 | RAG 코퍼스 | 최대 감점 |
|---|---|---|---|
| 전력 여유 | check_power_headroom | 이슈 사례집(트랜지언트·그룹레귤레이션) | −40 |
| CPU↔GPU 병목 | check_bottleneck | 벤치 노트 | −20 |
| BIOS 의존 | check_bios_support | 이슈 사례집(BIOS 의존성) | −15 |
| 발열·스로틀 | (리뷰축) | 이슈 사례집(GPU 발열, 케이스 쿨링) | −15 |
| 물리 간섭 | check_physical_fit | 케이스 클리어런스 사례 | −30 |
| RAM QVL/EXPO | — | RAM QVL 사례 | −10 |

### 10-5. 유아 품목별 검증 축 + 감점 배점표 초안
| 축 | 도구 | RAG 코퍼스 | 최대 감점 |
|---|---|---|---|
| 리콜 이력 | lookup_recall | 리콜 상세 | −60 |
| KC 인증 | lookup_kc_cert | — | −35 |
| 월령 적합성 | check_age_fit | 안전 가이드라인·발달 가이드 | −25 |
| 안전 사용(끼임·질식) | — | 사용설명서·안전 가이드라인 | −20 |
| 리뷰 진위 | get_review_authenticity | — | −10 |

- 리콜 경고 배지 = 즉시 노출(채점 결과 무관), 검증은 계속 진행. 인용마다 출처 URL + `collected_at`.

### 10-6. 리뷰 진위 축 — 인터페이스 계약
```python
def get_review_authenticity(product_key: str) -> {
    "orig_rating": float, "cleaned_rating": float,
    "cleanse_ratio": float,               # 0~1
    "axis_scores": { <축>: {"label": "긍정|부정", "pct": int, "n": int} },
    "total_reviews": int,
    "top_summaries": [ {axis, sentiment, text, source_label, source_url, collected_at} ],
    "evidence_cards": [ {claim, observed, baseline} ],   # 반박 가능한 관측 사실
    "confidence_note": str                 # "샘플 부족" 등 — judge가 참고
}
```
- [3-C] judge: `cleanse_ratio > 0.20` → 리뷰 근거 가중치 하락 + 리뷰 진위 축 소폭 감점(최대 −10).
- `evidence_cards`는 화면 S4-a 추론 로그와 [5] 설명에 그대로 인용된다.
- **산출 로직(오프라인 클렌징·진위 라벨링·`cleanse_ratio` 정의)은 §13.** [3-C]는 위 dict만 소비한다.

### 10-7. 출력
```jsonc
VerificationResult = {
  list_id, category, mode,          // "set" | "per_item"
  targets: [ {
    subject: "세트 전체" | "젖병소독기 후보 A",
    confidence: 86, passed: true, checks: 6,
    issues: [ { axis, 관측, tool_result,
                evidence:[{snippet, source_url, collected_at}],
                judge:"경미", penalty:4, narration:"..." } ],
    gray_axes: ["RAM QVL — 근거 0건"],
    verify_log: [...]
  } ],
  verified_at
}
```

### 10-8. 재탐색 연결
| | 트리거 | 동작 | hardcap |
|---|---|---|---|
| 컴퓨터 | 세트 confidence < 80 | 최대 감점 축 슬롯 → `[4]`(exclusion) → 재최적화 → 재검증 | 3회 |
| 유아 | 품목 confidence < 80 | 그 품목 `[3-B]` 다음 후보 → 재검증 | 품목당 3회 |

**3회 초과**: 컴퓨터 = best-so-far + "검증 미완료" 표시 통과 / 유아 = 안전축(리콜·KC·월령) 미해결 → **제외**, 그 외 축 → 표시 통과.

### 10-9. 데모 = 시나리오별 정답값 주입

> **[3-C] 데모는 실제 탐지 성능이 아니다.** "검증 파이프라인이 근거를 제시하며 도는 것"을 보이는 용도.

| 요소 | 데모 처리 |
|---|---|
| `@tool` 함수 (`check_power_headroom` 등) | 시나리오별 **고정값 반환** (`_MOCK_TOOL_RESULTS`) — 스켈레톤 `_MOCK_CONFIDENCE_BY_PRODUCT` 패턴 확장 |
| `get_review_authenticity` | **사전계산된 `review_summaries` 값**을 읽음 (§13-1 실측과 무관한 데모용 시드) |
| RAG 코퍼스 | 시나리오에 맞춘 20~50 청크 미니 코퍼스 |
| confidence | 재탐색 1회 실패 후 통과하도록 **시드** |
| **쟁점 문장** | Bedrock(Haiku)으로 **실제 생성** — "AI가 근거 대며 검증"이 화면에 보임 |
| `VerificationResult` 스키마 · tool 시그니처 · `evidence_search` 계약 · judge 알고리즘 | **실제 것 유지** → 나중 drop-in |

데모 시나리오별 정답값 세트 (최소 3종):
1. **컴퓨터 세트 통과** — 롤·옵치 150만원, confidence 86, 회색축 1개(RAM QVL)
2. **컴퓨터 재탐색 1회 후 통과** — GPU 발열 쟁점 → 슬롯 교체 → 재구성 → 통과 (목업 3e 재탐색 로그 시연)
3. **유아 품목 리콜 제외** — 후보 A 리콜 히트 → 배지 + 제외 → B 추천

### 10-10. 상세 설정 — 미작성 (팀 작업 필요)
아래는 §10-1~10-9의 구조 위에 채워야 할 구현 상세. **현재 공백.**
| 항목 | 내용 |
|---|---|
| judge 알고리즘 | 관측값 파싱 → 채점 → 집계 의사코드, 부분점수 규칙(도구 "위반 명확" vs "애매") |
| 쟁점(issue) 스키마 | 관측 · 근거 · 판정 · 감점 구조 |
| 쟁점 문장화 프롬프트 | 실제 문안 (입력 = 관측값·근거, 출력 형식, **판정어 금지**, 환각 억제) |
| 도구 호출 오케스트레이션 | 축별 호출 순서·병렬화·타임아웃·재시도 한도 |
| 재검증 전이 조건 | "미해결 축" 판별, 재탐색 트리거, 조기 종료 |
| evidence_search 쿼리 생성 | 축 → 쿼리 템플릿 |
| verify_log 저장 형식 | S4-a 추론 로그 렌더용 구조 |
| MOCK_MODE 분기 | Bedrock 미연결 시 축별 목값 |

### 10-11. 엣지 케이스
| # | 상황 | 처리 |
|---|---|---|
| E1 | evidence_search 전부 0건 | 전 축 회색 → "규칙 검증만 통과" |
| E2 | 도구 결과 ↔ RAG 근거 상충 | 중간 감점 + 회색 표시 |
| E3 | 도구가 "위반" 확정 | 해당 축 감점 확정 |
| E4 | Bedrock 타임아웃 | 문장화 실패 → **규칙 템플릿 문장으로 degrade**. 신뢰도는 영향 없음 |
| E5 | 리뷰 데이터 없는 신제품 | 리뷰축 회색 (`confidence_note: "샘플 부족"`) |
| E6 | 유아 리콜 히트 | 배지 즉시 + 검증 진행 |
| E7 | 컴퓨터 재탐색 같은 슬롯 계속 지목 | [3-0] 재수집 or 완화 요청 |
| E8 | 재탐색 무한루프 | exclusion 누적 → 후보 고갈 시 종료 보장 |

### 10-12. 비용 (데모)
검증 1건 ≈ Haiku **1~2회**(쟁점 문장화) + evidence_search 임베딩 수건 ≈ $0.01~0.03. 컴퓨터(세트 1 × 재탐색 최대 3) ≈ $0.03~0.09. 유아(품목 4~8) ≈ $0.04~0.12. 디베이트를 걷어내면서 [3-C] 비용이 약 1/2로 줄었다.


---

## 11. 엔진 [5] 설명 생성

### 11-1. 입출력
- 입력: `BuildResult`/`BasketResult` + `VerificationResult` + `RankResult` breakdown + `review_summaries` + `RequirementSpec`
- 출력: `Explanation` — 헤더 요약 1개 + 항목별 이유 1줄 + 속성 기여도(%) + 근거 인용
- 렌더: S4 카드 추천이유 / 헤더 요약 · S4-a 로그

### 11-2. (a) 속성 기여도 — 계산 (LLM 아님)
```
contribution[축] = Σ_item (slot_weight_item · item.breakdown[축]) / total
축 매핑: 성능여유 → "성능" / 가격 → "가격" / (밸런스 + 호환여유) → "호환성"
→ { 가격: 41, 성능: 33, 호환성: 26 }
```
- **3축 고정.** 리뷰는 카드에 "실사용 평점 4.2" 별도 노출.
- 데모 = breakdown 집계 / 최종 = counterfactual perturbation 옵션.

### 11-3. (b) 문장 — Bedrock structured output (Haiku, 1회)
```jsonc
{ headline: str, items: [{slot, reason}], caveats: [str] }
```
프롬프트 입력: 확정 슬롯·assumed / BuildResult items+totals / VerificationResult(passed·confidence·issues·gray_axes) / item breakdown 상위 축 / RAG 근거 스니펫.

이유 생성 힌트:
| 상황 | 이유 방향 |
|---|---|
| rank 1 + 밸런스 부합 | "조건 충족, 예산 내 최적 밸런스" |
| 가격축 기여 큼 | "동급 성능 중 최저가" |
| bottleneck_hint 반영 | "GPU 의존도 높아 여유분을 GPU로" |
| 호환 제약으로 좁혀짐 | "선택한 메인보드 규격에 맞춘 구성" |
| 회색축 존재 | → caveats "RAM QVL 근거 확인 안 됨" |

### 11-4. RAG (정당화용)
`[3-C]`가 모은 `VerificationResult.issues[].evidence` 재사용. 부족 시 `evidence_search(domain, "왜 이 부품이 이 용도에 적합", filters)` 추가 1~2건. 근거 0건 → 규칙 기반만, `evidence: []`.

### 11-5. 출력
```jsonc
Explanation = {
  list_id,
  headline: "QHD 165Hz에서 두 게임 모두 안정적으로 돌아가는 조합입니다. 예산 150만원 중 1,482,000원 사용, 호환성 경고 없음.",
  contribution: { 가격: 41, 성능: 33, 호환성: 26 },
  items: [ { slot:"GPU", reason:"...", basis:["rank1","가격축"], evidence:[{snippet, source_url, collected_at}] }, ... ],
  caveats: ["RAM QVL 근거는 확인되지 않았습니다"],
  review_line_by_slot: { GPU: "리뷰 1,284건 · 조작 의심 12% 제외 · 실사용 평점 4.2" },
  generated_at
}
```

### 11-6. 환각 방지
수치·부품명·통과여부는 코드가 확정 → "이 값만 사용, 새 수치 생성 금지". 후처리: `items[].slot`이 실제 슬롯과 일치? 없는 부품명 없나? → 실패 시 1회 재생성 → **규칙 템플릿 fallback**: `"{slot}: {조건} 충족, {rank}순위, {가격}원"`.

### 11-7. 엣지
| # | 상황 | 처리 |
|---|---|---|
| E1 | 검증 미통과로 도달 | headline "일부 항목 검증 미완료" + caveats |
| E2 | RAG 전체 0건 | 규칙 기반 reason, evidence 빈 배열 |
| E3 | assumed가 근거 | reason에 "기본값 가정(FHD 144Hz)" 표기 |
| E5 | 업그레이드 모드 | headline "GPU만 교체 시…" + chain_warnings → caveats |
| E7 | contribution 쏠림 | 그대로 표시, "예산 제약이 컸다" 뉘앙스 |

### 11-8. 비용
Haiku 1~2회 (유아 2~3회) ≈ $0.005~0.02 / 건.

---

## 12. 엔진 [6] 사후 학습 (되먹임) — 별도 배치

요청 경로 밖. 엔진이 읽는 설정·룩업 테이블만 갱신. **데모 = 로깅 스키마만.**

### 12-1. 신호 수집
`feedback_events(id, list_id, user_id?, type, payload jsonb, ts)`:
| 신호 | 출처 | 의미 |
|---|---|---|
| recommend_shown | S4 | 기준선 |
| item_kept / item_removed | S4 담기/제거 | 수용/거부 |
| alt_viewed / alt_swapped | "대안 보기" | 1순위 불만족 |
| condition_edited | S3/S4 | 슬롯 수정 |
| research_requested | "다시 골라줘" | 재탐색 유발 |
| list_confirmed | S5-a | 최종 채택 (강신호) |
| buy_link_clicked | S5-b | 구매 의향 |
| purchase_verified | A7 | 실구매 (최강신호) |
| review_submitted | A7 | 크라우드 리뷰 (ground truth) |
| price_alert_hit / reacted | 이메일 | 가격 민감도 |

### 12-2. 학습 대상
| # | 대상 | 입력 | 방법 | 데모 |
|---|---|---|---|---|
| 2-1 | balance_profiles | list_confirmed 세트의 실제 (purpose, resolution, tier) | 중앙값 → ideal. EMA, ±0.5 클램프 | 수기 고정 |
| 2-2 | budget_profiles | 확정 세트 슬롯별 지출 비율 | 중앙값. EMA | 수기 고정 |
| 2-3 | DEFAULT_WEIGHTS | kept vs removed + breakdown | 로지스틱 회귀 → 계수 정규화 | 미실행 |
| 2-4 | game_requirements | 크라우드 제보 "잘 돌림/안 돌아감" | tier_min 조정. 검토 큐 | 미실행 |
| 2-5 | parts.perf_tier | 크라우드 벤치 제출 | 분포 재정규화. 제3자 점수 학습 입력 금지 | 미실행 |
| 2-6 | 리뷰 진위 파라미터 | purchase_verified + review_submitted | 라벨러 임계 튜닝. 텍스트 AI탐지 계속 최하위 | 미실행 (리뷰 담당) |
| 2-7 | doc_chunks (RAG) | 이슈 제보, 재탐색 반복 패턴, 신규 리콜 | 큐레이션 큐 → 사람 검토 후 추가 | 미실행 |

### 12-3. 안전장치
표본 최소 건수 / 변화폭 클램프 / **승인 게이트**(balance·budget·weights 자동 / game_req·perf_tier·RAG 승인 필수) / staging↔prod 롤백 / 콜드스타트 = 수기 초안 / 피드백 폭주 방지(top-N에 비주류 후보 ε-greedy, 최종만).

### 12-4. 출력물
| 테이블 | 갱신 | 읽는 곳 |
|---|---|---|
| balance_profiles | ideal_tier, offset | [3-B] |
| budget_profiles | alloc 비율 | [2], [4] |
| DEFAULT_WEIGHTS | 축 가중치 | [3-B] |
| game_requirements | tier_min (검토 후) | [2] |
| parts.perf_tier | 재정규화 (검토 후) | [2], [3-A] |
| doc_chunks | 신규 근거 (큐레이션 후) | [3-C], [5] |

---

## 13. 리뷰 클렌징 (오프라인 배치)

요청 경로 밖. `[3-B]` 랭킹의 리뷰 축과 `[3-C]`의 `get_review_authenticity`가 읽는 **`review_summaries`를 만드는 배치**다. 담당 = 리뷰 담당 팀원. **데모 = 사전계산 값 주입**(§10-9와 같은 원칙).

원문은 저장하지 않는다 — 수치·요약·근거 카드·출처만 남긴다(§16-4 저작권).

### 13-1. 탐지 5축과 실측 결과

평가 프로토콜은 **정상 리뷰 오탐률 1% 고정 시 탐지율**. 조작 비율이 낮아 정확도·F1은 무의미하다.

| 축 | 방법 | 실측 | 판정 |
|---|---|---|---|
| 광고성 | 공시 문구 준지도 학습 | 20만 건 중 실질 **0건** | 시드 부재 — 시작 불가 |
| AI 작성 (문자 통계) | 문자 5-gram LM `bpc` + 8지표 앙상블 | 단독 3.6% → 앙상블 **6.0%** | 기준(30%) 미달 |
| AI 작성 (어휘) | 어휘 계수 | **53.8%** — 도메인·문체·단일 저자 누출 3종 | 재검증 중 |
| **관계·행동** | 수작업 특징 15종 + 로지스틱 회귀 | **AUC 0.903** (텍스트 단독 0.901) | **주축 채택** |
| 허위 여부 | 리뷰 주장 ↔ 상품 스펙 대조 | 미착수 | **신규 개설** |

**보조 실측 (설계를 결정한 숫자들)**
| 관측 | 값 | 설계에 미친 영향 |
|---|---|---|
| 길이 분포 | 20자 미만 **30.9%**, 중앙값 29자 | 위양성 위험 구간 확인 |
| 짧은 리뷰 오탐 | 정상 대조군 대비 **1.8배** | 텍스트 축 단독 사용 금지 |
| 난독화 5겹 | 탐지율 **0.0%** | 텍스트 경로는 우회 저항 없음 |
| 길이별 탐지율 | 90~130자 구간 **1.4%** (가운데가 낮음) | "탐지율은 길이의 단조 함수" 가설 폐기 |
| 도메인 전이 | 오탐 1% → 실제 **0.1%** | 임계값 도메인별 재조정 필수 |
| 저비율 스윕 | 무너지는 건 비율이 아니라 **양성 절대 건수** | 데이터 평가 기준을 "건수"로 전환 |
| 메타·행동 11종만 | AUC **0.876** | 텍스트·그래프 없이도 대부분 나옴 |

### 13-2. 설계 원칙 (확정)

| 원칙 | 근거 |
|---|---|
| **LLM에게 "가짜냐"를 직접 묻지 않는다** | 직접 판정 정확도 50%, 틀리면서 확신. LLM은 "리뷰가 주장하는 사실" 추출에만 쓴다 — **판정이 아니라 추출** |
| **텍스트 단독으로 판정하지 않는다** | 난독화 5겹 → 0.0%. 작성 메타데이터·행동 신호 우선, 텍스트는 최하위 가중치 |
| **용도를 제한한다** | 자동 삭제·소비자 "AI 작성" 라벨 = 배제 / 랭킹 하향 = 단독 사용 금지 / 검토 큐 우선순위·캠페인 모니터링 = 채택 |
| **근거를 낼 수 없는 방법은 쓰지 않는다** | 성공 지표가 근거 채택률(§13-4) |
| **임계값은 도메인마다 다시 잡는다** | 전이 시 오탐 1%가 실제 0.1%로 동작해 조용히 눈감음 |

### 13-3. 파이프라인
```
수집 → 축별 점수 산출 → 결합기(로지스틱, GBM 교체 검토)
     → 도메인별 임계값 적용 → 평점 재계산(cleanse_ratio)
     → 근거 카드 생성 → 평가축별 대표 리뷰 추출 → 요약 3건
     → review_summaries 저장 (원문 미저장)
```
| 단계 | 방식 | 비고 |
|---|---|---|
| 축별 점수 | 관계·행동 = 수작업 특징 15종 / 텍스트 = 문자 LM·어휘 / 허위 = 스펙 대조 | 축마다 독립 산출 |
| 결합기 | 로지스틱 회귀 (현재) | GBM 교체는 비선형 상호작용 확인 후 (B3) |
| 임계값 | 도메인별 재조정 | `cleanse_ratio` 20%는 공통 초안값 |
| 평점 재계산 | 걸러낸 리뷰 제외 후 재집계 | `orig_rating` / `cleaned_rating` 둘 다 보관 |
| 근거 카드 | 관측 사실 + 기준선 | §13-4 |

### 13-4. 근거 카드 — 산출 형태를 먼저 고정한 이유

성공 지표는 탐지율이 아니라 **근거 채택률**이다. 검토자가 확인·반박할 수 있어야 한다.

| | 검토자가 보는 것 |
|---|---|
| 수작업 특징 | "이 상품 리뷰의 **68%가 7일에 몰림** (중앙값 11%)" |
| GNN 임베딩 | "이 노드의 128차원 벡터가 이상함" |

후자는 AI 축이 `bpc 3.42`로 실패한 것과 정확히 같은 실패다 — 확인도 반박도 못 한다.

**표현 학습(GNN)을 이번 사이클에서 보류하는 이유 4가지**
1. 근거를 못 낸다 (위)
2. 라벨을 더 요구하는데 **그게 이 프로젝트의 병목**이다 — 무너지는 건 양성 절대 건수
3. 그래프가 희소하다 — 리뷰어당 리뷰 **중앙값 1건**, 대부분 노드가 홀로 뜬다
4. 외부 보고가 나쁘다 — 백만 노드에서 대다수 OOM, 이상 비율 0.1%에서 재현율 0

**재개 조건 (미리 정해 둠)** — 셋 다 충족되면 재검토
- 양성 절대 건수 **수천 건 이상**
- 계정당 리뷰 **중앙값 3건 이상** (그래프가 성립해야 한다)
- **근거 생성 경로 확보** — 임베딩 대신 기여한 **이웃 서브그래프**를 뽑을 수 있어야 한다

순서: `기여도 분해 → 시간 정렬 특징 → 특징 확장 → GBM → 그때 GNN 재검토`

### 13-5. 검증 데이터

**보유분**
| 이름 | 언어·도메인 | 규모 / 라벨 | 비고 |
|---|---|---|---|
| 네이버 쇼핑 리뷰 | 한글 · 쇼핑 일반 | 200,000행 / 평점 | **최대 140자 하드 캡 · 3점 0건.** 카테고리 컬럼 없음 |
| NSMC 영화 리뷰 | 한글 · 영화 | 50,001행 / 감정 | 도메인 전이 1회용 |
| Hollenbeck 아마존 | 영어 · 아마존 | 381,734행 / 사람 라벨 80,281 | 조작 상품 **42.8% 농축**. 관계 축 주 실험셋 |
| 합성 리뷰 | 한글 · 공산품 | 450건 / synthetic | **단일 모델·단일 세션** |
| 리뷰 요약 데모 | 한글 · PC 부품 | 51상품 / 생성값 | 원문 미저장. 출력 스키마 검토용 |

**후보를 평가하는 축** — "있다/없다"로 잡으면 140자 캡 같은 함정을 또 놓친다.

| 관계 등급 | 조건 | 열리는 것 |
|:---:|---|---|
| 0 | 없음 | — |
| 1 | 작성자 ID | 개인 기준선(문체 편차) |
| 2 | + 타임스탬프 | 시간 몰림(`burst7`) |
| 3 | + 계정당 리뷰 **중앙값 2건 이상** | 공유 리뷰어 그래프 |
| 4 | + 상품 메타 연결 | 상품→리뷰 2단계 |

> Hollenbeck은 `reviewer_id`가 100% 있는데도 **2등급**이다 — 리뷰어당 중앙값 1건이라 그래프가 별 모양.

**라벨 근거 등급** — 등급이 다른 라벨을 한 칸에 섞지 않는다.
`transaction_verified`(내부 확인) > `investigated`(조사 확정) > `filter_inferred`(운영 필터 추정 — 정답 아님) > `synthetic`(합성)

**수집 후보**
| 이름 | 언어·도메인 | 규모 / 라벨 | 여는 실험 |
|---|---|---|---|
| **KatFish** | 한글 · 에세이~시 | 2,094건 / synthetic **4모델** | **미관측 모델 홀드아웃** · 긴 글 구간(600자+) |
| RAID | 영어 · 8장르 | 600만+ / synthetic 11모델 | 표준 **적대적 공격 11종**으로 난독화 저항성 재측정 |
| **AI Hub 상품 리뷰** | 한글 · IT·패션·화장품·가전·생활 | 250,312건 / 감정 + 속성 span | 도메인 매칭 대조군 · 허위 축 입력. **유아 없음**, `IT>컴퓨터`가 최근사 |
| YelpZip / YelpChi | 영어 · 식당 | 리뷰어 260,277 / `filter_inferred` | 관계 **3등급** — 관계 축 특징 확장 |
| **Amazon'23 Baby·Electronics** | 영어 · 유아·전자 | 리뷰 6.0M / **조작 라벨 없음** | 상품 스펙 대조 · `verified_purchase` 구매 확인 축 · **도메인 짝** |
| 모두의 말뭉치 / AI Hub 웹 말뭉치 | 한글 · 다양 | 대규모 / 라벨 불필요 | 문자 LM 재학습 → 도메인 전이 완화 |

**확정된 제약 2가지**
- **한글 · 리뷰 · 관계 정보가 동시에 있는 공개 데이터가 없다.** AI Hub 상품 리뷰에 작성자 ID가 없어 계정을 이을 수 없다(관계 등급 0). → **한국어 관계 축은 내부 데이터 전까지 불가.** 공개 데이터로는 파이프라인과 특징을 영어로 완성해 둔다.
- **한국어 유아용품 리뷰 공개 데이터셋도 없다.** 유아는 영어(Amazon'23)로만 가능하고, PC는 AI Hub `IT>컴퓨터`로 한국어 근사가 된다.

**받을 때 반드시 할 검사** — 네이버 코퍼스에서 140자 캡과 3점 0건을 컬럼만 보고 오래 놓쳤다.
| 검사 | 판정 |
|---|---|
| 절단 | 최상위 길이 구간 건수가 그 아래보다 많으면 절단 의심 |
| 큐레이션 | 평점·라벨의 어느 값이 0건이면 경고 |
| 타임스탬프 | 없으면 시간 기준 분할 불가 → 그 데이터로 낸 수치는 보고하지 않는다 |
| 길이 분포 | 20/40/60/100/200/500/1000자 구간 비율을 항상 기록 |
| 라이선스 | 재배포·저장소 커밋 가부 명시 |
| 중복 | 동일 텍스트 재사용 비율 |

> **원문은 저장소에 커밋하지 않는다. 받는 스크립트만 커밋한다.** 수집 단계에서 원본 컬럼을 깎지 않는다.

### 13-6. 검증 일정 — 개발(W1~W4)과 별개 트랙

**9/15까지** — 지금 가진 데이터로 돌릴 수 있는 것만
| # | 실험 | 답하는 것 |
|---|---|---|
| B1 | **어휘 축 문체 통제 재측정** | 문어체 대조군으로 문체 축을 죽였을 때 남는 **어휘 신호의 하한**. 30% 초과 시 "텍스트 단독 판정 금지" 원칙 재검토 |
| B2 | 메타·행동 11종 기여도 분해 | `burst7`(7일 몰림) 하나로 되는가 — 되면 타임스탬프만으로 축이 선다 |
| B3 | 결합기 로지스틱 → GBM | 비선형 상호작용에 신호가 남아 있나 |
| B4 | **시간 정렬 특징 신설** | "같이 나타났는가"가 아니라 "**같은 순서로** 나타났는가" — 우연과 조율을 가르고, 근거로도 좋다 |

**9/16 ~ 10/26**
| 기간 | 초점 |
|---|---|
| 9/16 ~ 9/30 | 어휘 축 정식화(도메인 매칭 대조군) · 관계 축 특징 확장 · 학습화 타당성 판정 |
| 10/1 ~ 10/15 | 긴 글·**미관측 모델 홀드아웃** · 상품 메타데이터 대조 시범 |
| 10/16 ~ 10/26 | 통합 · 축별 재판정 · 문서 갱신 |

**관계 축 특징 확장 후보** — 전부 수작업 특징이고 전부 근거로 쓸 수 있다.
시간 정렬 / 이웃 상품의 평점 분포 / 리뷰어 신규성 / 평점 궤적 / 리뷰 간 시간 간격 분포

**허위 여부 축** — 리뷰가 그 상품에 없는 것을 칭찬하는가. ("무선이라 선 없어서 좋아요" ← 유선 제품)
문체와 무관해서 텍스트 축을 무너뜨린 공격이 전부 안 통하고, 근거가 자동으로 나온다. 스펙이 정형인 PC 부품부터 시작.

### 13-7. 이번 사이클 범위 밖

빠뜨린 것이 아니라 **의도적으로 뺀 것**이다.

| 항목 | 왜 뺐나 | 남겨 둘 것 |
|---|---|---|
| **리뷰 폼 텔레메트리** | 계측 코드 배포 + 2~4주 축적이 필요 | **소급 수집이 불가능하다.** 실서비스 도입 결정 시점에 **가장 먼저 켜야 한다** |
| 내부 리뷰·캠페인 데이터 | 승인 절차가 기간을 넘는다 | 한국어 관계 축의 유일한 경로 |
| 검토 큐 무작위 대조 표본 | 검토 큐 자체가 없다 | **큐를 만드는 시점에 정해야 한다.** 나중엔 못 만든다 |
| 사람 상한(κ) 측정 | 검토자 2명 필요 | 다음 단계 |

> 텔레메트리를 뺀 것이 "안 중요하다"는 뜻이 아니다. 문체를 어떻게 바꿔도 **"붙여넣었다"는 사실은 안 바뀌므로**, 텍스트 축이 무너진 자리를 메울 후보다.

---

## 14. RAG 공통 컴포넌트

### 14-1. 구성
- **Bedrock Titan Text Embeddings v2** + **PostgreSQL `pgvector`**. (Bedrock Knowledge Base 아님)
- 코퍼스 테이블:
```sql
doc_chunks (
  id, domain, doc_type, product_key null, age_month_range null,
  text, embedding vector, source_url, collected_at
)
```

### 14-2. 하이브리드 검색 (한 쿼리)
```sql
WHERE domain = :d
  AND (product_key = :pk OR product_key IS NULL)
  [AND age_month_range @> :month]
ORDER BY embedding <=> :qvec
LIMIT :k
```

### 14-3. 단일 컴포넌트, 소비처 2곳
같은 `evidence_search(domain, query, filters)` `@tool`:
- `[3-C]` 검증기 (쟁점 근거) · `[5]` 설명기 (정당화 근거)
- (선택) `[2]` 유아 월령 가이드라인 — 최종만
- **근거 0건 → "검증 불가(회색)"**

### 14-4. 코퍼스 (도메인당 데모 20~50 큐레이션)
- 컴퓨터: 이슈 사례집(BIOS 의존성, 파워 트랜지언트, GPU 발열 스로틀, 케이스 클리어런스, RAM QVL) + 벤치 노트 + 설치 매뉴얼 발췌
- 유아: 리콜 상세 + 안전 가이드라인 + 발달 가이드 + 사용설명서 발췌

### 14-5. 저작권
원문 저장·표시 안 함. 저작물은 **사실 발췌 + 우리 요약 + 출처 링크만, 원문 미저장**. 제3자 벤치는 단건 인용 + 링크, 대량 추출 금지 (잡코리아 v 사람인 2017 판례).

### 14-6. RAG 미사용 단계
`[1]`, `[3-A]`, `[3-B]`, `[4]` — 전부 결정적 SQL/규칙.

---

## 15. 데이터 모델

### 15-1. 데모 최소 (실제로 데모를 돌리는 테이블)
```
users, auth_codes,
lists, messages,
requirement_specs, hard_filter_results, rank_results,
build_results, verification_results, explanations
sessions/quotes/verdicts   -- 스켈레톤 호환용
```

### 15-2. 최종 추가
```
lists(status 확장), items, reasoning_logs,
review_summaries (리뷰 담당), price_alerts, price_history, panel_prefs,
doc_chunks (RAG), feedback_events (되먹임),
-- 도메인 룩업
parts_catalog(스펙·perf_tier·가격), game_requirements, balance_profiles, budget_profiles,
bios_compat, scaling_factors, psu_headroom,
age_fit_table, category_item_map, kc_certs, recalls, baby_products
```

### 15-3. 핵심 스키마 발췌
```sql
users (id uuid pk, email text unique, created_at, last_login_at, display_name null)
auth_codes (id, email, code_hash, expires_at, attempts int, consumed bool, created_at)

lists (
  id, user_id null, browser_token, category, mode,
  status, objective_text, slots jsonb, created_at, updated_at
)

review_summaries (   -- 리뷰 담당 팀원 산출 / [3-B]·[3-C]가 소비
  product_key, category, total_reviews, cleanse_ratio,
  orig_rating, cleaned_rating, axis_scores jsonb,
  top_summaries jsonb, sources jsonb, collected_at
)
-- 원문 미저장. 요약·수치·출처만.
```

---

## 16. 데이터 소스 · 합성 데이터 정책

### 16-1. 배경 — 네이버쇼핑 API 종료
네이버쇼핑 검색 오픈API가 종료됐고, 대체 API(쿠팡 파트너스·11번가/G마켓 제휴·다나와/에누리 가격비교·어필리에이트)는 **전부 판매자·제휴 관계가 전제**. 자체 크롤링은 저작권 원칙(대량 추출 금지)과 충돌.
→ **데모 = 전부 합성/큐레이션. 최종 = 제휴 API 확보가 전제.**

### 16-2. 데모 합성 데이터 목록

| 데이터 | 스키마 | 담당 | 상태 |
|---|---|---|---|
| **컴퓨터 `parts_catalog`** | `type, product_key, name, brand, <스펙 필드>, perf_tier, base_price` + 판매필드 | 스펙·perf_tier 초안 = 본 문서 / 판매필드 = 생성기 / 검토 = 팀 | 예정 |
| **유아 `baby_products`** | `product_key, category, sub_item, name, brand, price, market_avg_price, age_month_min/max, kc_cert_no, recall_flag, product_desc, ...` | 유아 담당 팀원 | 작성 중 → 전달 예정 |
| **`review_summaries`** | §15-3 | 리뷰 담당 팀원 (§13). AI Hub 상품 리뷰 참고 + 합성. **관계·행동 축 시연을 위해 리뷰어 ID·작성 시각·상품 ID와 캠페인 패턴(단기 몰림·순서 반복) 포함** | 진행 중 |
| **game_requirements / balance_profiles / age_fit_table / recalls / kc_certs** | §5, §8 | 팀 분담 (§19) | 예정 |

### 16-3. `parts_catalog` 생성 방식 — 하이브리드
| 필드군 | 방식 | 이유 |
|---|---|---|
| 스펙 (socket, tdp_w, mem_type, length_mm, vram_gb, wattage …) | **수기 큐레이션** (본 문서 초안) | 실제 사실 — 틀리면 [3-A]/[4] 무의미. 랜덤 생성 불가 |
| perf_tier (1~10) | 수기 (game_requirements·balance_profiles와 같은 사다리) | 판단 필요 |
| 판매필드 (price, market_avg_price, product_desc, image_url, mall_name, url, in_stock) | **생성기** (`base_price` 실제 시세 근사 → ±지터, market_avg ×1.05~1.15, desc 템플릿, in_stock 1~2개 false) | 보일러플레이트·반복. 재현 가능 |

파일:
```
data/parts_catalog.csv          # 수기 큐레이션
scripts/gen_parts_offers.py     # 판매필드 자동 부여
data/parts_catalog_full.json    # [3-0]이 읽는 최종형
```

### 16-4. 저작권 준수
- 리뷰 원문·제3자 콘텐츠 원문 **미저장** — 발췌 + 요약 + 출처 링크만
- AI Hub 상품리뷰 데이터 사용 시 해당 데이터셋 '저작권 및 이용정책' 상업적·2차가공 가능 여부 확인 (데모=비상업)
- 합성 상품 설명·가격은 자체 생성물

---

## 17. AWS 구성 (요약 — 상세는 별도 구성도)

### 17-1. 데모
- EC2 1대 (`t3.small`/`t4g.small`) + Docker Compose: FastAPI + PostgreSQL 16(pgvector) + cron 워커 + React 정적(nginx/caddy)
- VPC 1서브넷. **RDS ✕ / ALB ✕ / NAT ✕**
- IAM 인스턴스 롤 → boto3 자동 자격증명
- **SES 샌드박스 — 로그인 6자리 코드 발송** (시연 계정 사전 인증) + 콘솔 로그 fallback
- SSM Parameter Store — **네이버 API 키 제거** (데모는 합성 카탈로그). Bedrock 리전/모델 ID 등만
- S3 (데이터 자산 + pg_dump 백업). 인스턴스 stop (terminate 금지)
- 비용 ~$5~20/주 (Bedrock만 프리티어 없음, Haiku 저렴)
- 리전: us-east-1 기본 / 서울도 가능 — Claude Haiku Model access 확인. pgvector라 S3 Vectors 리전 제약 없음. 모델 ID env var

### 17-2. 최종
- ECS Fargate (api + worker 분리), CloudFront + S3(React), ALB, VPC 2 AZ
- RDS PostgreSQL Multi-AZ (또는 Aurora Serverless v2, pgvector)
- EventBridge Scheduler → Lambda (가격 폴링 = 제휴 API), SQS → worker (리뷰 클렌징·[6] 배치)
- SES production + DKIM/SPF, ECR, CloudWatch, GitHub Actions CI/CD, VPC endpoints
- 비용 ~$150~400/월

---

## 18. 데모 vs 최종 종합 비교표

| 영역 | 데모 | 최종 |
|---|---|---|
| 데이터 소스 | **전부 합성/큐레이션** (네이버 API 종료) | 제휴 API + 자동 수집 + 크라우드 |
| 조건 수집 UX | 대화 주도 + 인라인 칩 (칩 클릭=결정적) | 동일 |
| 로그인 | 이메일 6자리 → JWT 2주 / SES 샌드박스 | + refresh 토큰·폐기 목록 / SES production |
| [1] 기본값 | 동적 + yaml fallback | + 학습 |
| [2] perf_tier | 이산 1~10 수기 | 연속 0~100 벤치 정규화 |
| [2] game_requirements | 48행 수기 (8게임) | 100+ 반자동 + 갱신 |
| [2] BIOS | chipset 근사 ~7규칙 | 메인보드별 CPU 지원 |
| [3-0] 후보 | 합성 카탈로그 (parts_catalog / baby_products) | parts DB ⨝ 제휴 API ⨝ 공공 API |
| [3-A] 유아 리콜/KC | 큐레이션 룩업 | 공공데이터포털 API |
| [3-B] 리뷰 | 팀원 산출물 (A안 합성) | 오프라인 클렌징 배치 |
| [3-B] balance_profiles | 5행 수기 초안 | [6] 학습 |
| [4] 컴퓨터 탐색 | 완전탐색 27k | beam/ILP, 주변기기 슬롯 |
| [4] 케이스 | 고정 (ATX 미들타워) | 실제 슬롯 |
| [3-C] | **시나리오별 정답값 주입 + 쟁점 문장만 Bedrock 생성** | 실제 도구·RAG 연동 |
| [3-C] 모델 | Haiku (쟁점 문장화) | 문장화 Sonnet 승격 검토 |
| [3-C] RAG 코퍼스 | 도메인당 20~50 큐레이션 | 수백~수천, 자동 수집 |
| [5] 기여도 | breakdown 집계 | perturbation 옵션 |
| [6] | 로깅 스키마만 | 주 1회 배치 + 승인 대시보드 |
| 인프라 | EC2 1대 + Compose | ECS Fargate + RDS + CloudFront |
| 재탐색 hardcap | 3회 | 3회 (동일) |
| 리뷰 클렌징 | 사전계산 값 주입 (§13) | 실제 배치 가동 + 검토 큐 |

---

## 19. 미결정 사항 / 다음 회의 안건

### 19-1. 화면·[1]·로그인
- [ ] 카테고리 중간 변경 시 기존 slots 처리 (폐기 + 재생성?)
- [ ] 도메인 밖 입력 가드 방식
- [ ] messages 저장 — 별도 테이블 확정
- [ ] 인라인 칩 전환에 따른 목업 A3 카드 블록 → 칩 UI 조정 (버튼·우패널·A4~ 유지)
- [ ] 로그인: JWT 저장 방식 (httpOnly 쿠키 vs Authorization 헤더) 확정

### 19-2. 데이터 큐레이션 분담
- [ ] `parts_catalog` 스펙 초안(본 문서) → 팀 검토 (특히 perf_tier 서열)
- [ ] `parts_catalog` 판매필드 생성기 (`gen_parts_offers.py`)
- [ ] `baby_products` 카탈로그 (유아 담당 팀원) — 완성 시 전달
- [ ] `review_summaries` (리뷰 담당 팀원) — §13 설계·제약 반영, 캠페인 패턴 포함 합성
- [ ] `game_requirements` 48행 수기 담당
- [ ] `balance_profiles` 5행 초안 검토
- [ ] `age_fit_table` (~25행) 담당
- [ ] RAG 코퍼스 도메인당 20~50건 수집 담당 (저작권 규칙 준수)
- [ ] 유아 `recalls`·`kc_certs` 큐레이션 담당

### 19-3. [3-C] 상세 설계 (§10-10 공백)
- [ ] judge 알고리즘 의사코드 + 부분점수 규칙
- [ ] 쟁점 문장화 프롬프트 문안 (**판정어 금지**, 입력 = 관측값·근거)
- [ ] 쟁점 스키마 · 도구 오케스트레이션 · 재검증 전이 · verify_log 형식
- [ ] 데모 정답값 세트 3종 (`_MOCK_TOOL_RESULTS` + 미니 코퍼스 + confidence 시드)
- [ ] `evidence_cards` 렌더 형식 확정 (S4-a 추론 로그 · [5] 설명 인용)

### 19-4. 정책 확인
- [ ] `[3-C]` 감점 배점표 초안 (§10-4, §10-5) 최종 승인
- [ ] `cleanse_ratio` 임계 20% — 도메인별 재조정 시점 결정 (§13-2)
- [ ] 리전 최종 결정 (Bedrock Haiku Model access 확인 후)

### 19-5. 스켈레톤 마이그레이션
- [ ] `C:\project_file\final_demo\` (현재 캠핑 도메인) → Plan Basket 2도메인 + Strands 도구 오케스트레이션([3-C]) + pgvector RAG 구조로 이관 계획
