# TrueFit 개발 역할 분담

작성일: 2026년 9월 11일  
인원: 5명  
목표: PC·유아용품의 조건 수집 → 추천 → 근거 확인 → 리스트 확정·리포트 흐름을 실제 DB와 API로 연결한다.

## 범위 결정

- 프론트엔드는 현재 디자인을 유지하고 신규 화면·컴포넌트·애니메이션·다국어 고도화를 중단한다.
- 프론트 작업은 누락 파일과 잘못된 링크 정리, 실제 API 연결, 오류 처리와 E2E 검증으로 제한한다.
- **가격 알림 기능은 개발 범위에서 완전히 제외한다.**
  - 목표가 알림 API, 가격 주기 조회, 도달 판정, 이메일 발송, 알림 UI와 관련 워커를 구현하지 않는다.
  - `POST /lists/{id}/alert`를 서비스 계약과 완료 기준에 포함하지 않는다.
  - 리포트는 확정 당시 가격과 목표금액을 보여줄 수 있지만 이후 가격을 추적하거나 알림을 발송하지 않는다.
  - 기존 `notification.*` 설계는 현재 개발에서 읽기·쓰기·적재하지 않는다. 물리 삭제는 기존 DB 데이터 확인 후 별도 스키마 마이그레이션으로 결정한다.
- 한 명은 리뷰 클렌징 연구를 전담하며 다른 기능 구현 인력으로 전환하지 않는다.

## 담당 1 — 서비스 통합·프론트 연결

**소유 영역**

- `frontend/**`
- 프론트 API 어댑터와 브라우저 E2E 테스트
- 정적 파일 서빙·개발 실행·외부 기기 접속 검증

**할 일**

1. `TrueFit.html`과 현재 JavaScript 구조를 하나의 실행 가능한 진입 방식으로 정리한다.
2. 존재하지 않는 HTML·`js/pages/**` 참조를 제거하거나 현재 파일에 맞춘다.
3. 인증, 세션, 조건, 추천, 후보 교체, 리스트 확정, 리포트 API를 기존 화면에 연결한다.
4. 로딩·빈 결과·401·404·409·422·500 상태를 화면에서 처리한다.
5. 실제 백엔드를 대상으로 핵심 사용자 흐름 E2E를 작성한다.

**하지 않는 일**

- 디자인 개편, 신규 화면 기획, 애니메이션·반응형·다국어 고도화
- 백엔드 서비스와 추천/RAG 내부 구현
- 가격 알림 UI 및 호출

## 담당 2 — 인증·세션·리스트 백엔드 및 DB 통합

**소유 영역**

- `src/api.py`, `src/auth/**`, `src/db/**`
- `src/routers/auth.py`, `src/routers/session.py`, `src/routers/lists.py`
- 인증·세션·리스트 서비스와 사용자·계획 repository
- `db/migrations/**`, DB 명세와 설계 근거

**할 일**

1. DB 연결 풀과 요청 단위 트랜잭션을 구현한다.
2. 이메일·비밀번호 인증과 httpOnly 쿠키를 구현한다.
3. 게스트 소유권 확인과 로그인 후 대화·계획 귀속을 구현한다.
4. 세션 생성, 조건 저장, 메시지 저장, 리스트 조회·이름 변경·소프트 삭제를 구현한다.
5. 추천 담당과 RAG 담당의 서비스를 API 흐름에 주입한다.
6. 리스트 확정과 읽기 전용 리포트를 구현한다.
7. 다른 담당자의 스키마 요청을 검토하고 마이그레이션을 최종 작성한다.

**하지 않는 일**

- 추천 점수·최적화 알고리즘 및 RAG 검색 내부 구현
- 가격 추적·판정·알림 API, notification worker 구현

## 담당 3 — 추천 엔진·카탈로그

**소유 영역**

- `src/pipeline.py`, `src/engine/**`, `src/categories.py`
- `config/categories/**`
- catalog/product/engine repository
- PC·유아용품 카탈로그 생성 및 멱등 적재 도구

**할 일**

1. 시나리오 정답 주입을 규칙 기반 조건 추출로 교체한다.
2. PC와 유아용품의 질문·필수 조건·슬롯 정의를 완성한다.
3. CSV 직접 조회를 DB 카탈로그 후보 조회로 교체한다.
4. PC 필터·순위·세트 최적화와 유아 품목별 추천·예산 배분을 구현한다.
5. 후보 교체, 선택 수량과 구매 시점을 반영한다.
6. 추천 실행·후보·검사 결과를 영속화하고 조회 DTO를 제공한다.

**제공 인터페이스**

~~~python
start_recommendation(revision_id, rag_service) -> run_id
get_result(run_id) -> RecommendResult
get_alternatives(requirement_id) -> list[Alternative]
swap_candidate(requirement_id, candidate_id) -> RecommendResult
~~~

**하지 않는 일**

- RAG SQL·임베딩·인용 권한 로직
- 리뷰 클렌징 모델과 라벨 정책
- 가격 알림을 위한 가격 재조회

## 담당 4 — RAG·상품 근거

**소유 영역**

- `src/rag/**`, `src/repo/rag_repo.py`
- 상품 자료와 근거 관련 repository
- `scripts/rag_manual.py`, 설명서 적재 도구
- `tests/test_rag*`, `docs/rag_implementation.md`

**할 일**

1. 설명서·상품 자료의 적재, 청크화와 임베딩을 유지한다.
2. pgvector와 키워드 하이브리드 검색을 제공한다.
3. 상품·옵션·월령·사용 조건의 적용 범위를 검사한다.
4. 검색 전후와 인용 반환 직전에 권한·철회 상태를 확인한다.
5. 추천 엔진이 직접 DB를 조회하지 않도록 안정적인 `RagService` 계약을 제공한다.
6. 임시 PostgreSQL과 운영 호환 DB에서 통합 테스트를 유지한다.

**하지 않는 일**

- 최종 추천 점수와 pass/fail 결정
- 리뷰 클렌징 및 가격 추적

## 담당 5 — 리뷰 클렌징 연구

이 역할은 전담이며 다른 일정이 늦어져도 일반 백엔드 구현으로 전환하지 않는다.

**소유 영역**

- 리뷰 정제·조작 의심 판정 연구
- `src/services/review_service.py`
- review/dataset repository와 `src/workers/review_cleanse_worker.py`
- 리뷰 데이터셋·라벨·평가 보고서

**할 일**

1. 리뷰 수집·보관·요약에 허용되는 범위를 정의한다.
2. 중복, 광고성, 조작 의심, 정보 부족 신호를 연구한다.
3. retained/excluded/undetermined 판정 기준과 사람 검수 지침을 만든다.
4. 정제 전후 평점·분포와 상품별 요약을 생성한다.
5. 실제 리뷰와 합성 표본을 격리하고 학습·평가 분할 누출을 검사한다.
6. `GET /reviews/summary/{product_key}`가 읽을 수 있는 검증된 집계 결과를 제공한다.

**하지 않는 일**

- 추천 순위 직접 조정
- 상품 설명서 RAG와 가격 알림

## 충돌 방지 규칙

1. `db/migrations/**`는 담당 2만 최종 수정한다. 다른 담당자는 필요한 필드와 제약을 문서로 요청한다.
2. 공통 요청·응답 DTO는 담당 1·2·3·4가 합의하고 담당 2가 최종 반영한다.
3. 담당 3은 RAG 테이블을 직접 조회하지 않고 담당 4의 `RagService`만 호출한다.
4. 담당 4는 근거와 확인 상태를 반환하고 최종 추천 판정은 담당 3이 수행한다.
5. 담당 5는 리뷰 집계 결과만 제공하며 추천 알고리즘을 직접 변경하지 않는다.
6. 담당 1은 API 내부를 수정하지 않고 계약 위반을 재현 가능한 E2E 실패로 전달한다.
7. 가격 알림 관련 화면·API·worker·테스트는 어느 담당자의 작업 목록에도 추가하지 않는다.

## 통합 순서

~~~text
담당 2: DB 연결·인증·세션·계획 API
   ├── 담당 3: DB 카탈로그·추천 엔진
   ├── 담당 4: RAG·상품 근거 서비스
   └── 담당 5: 리뷰 클렌징·집계
                    ↓
담당 2: 추천·근거·리뷰 서비스 조립
                    ↓
담당 1: 기존 UI 연결·E2E·실행 환경 검증
~~~

## 공동 완료 기준

1. 새 DB에 마이그레이션과 멱등 데이터 적재가 성공한다.
2. PC 신규 구성 한 건이 세션 생성부터 리포트까지 실제 DB로 완주한다.
3. 유아용품 한 건이 조건 수집부터 품목별 추천과 근거 표시까지 완주한다.
4. 추천 결과의 상품·가격·리뷰·RAG 근거가 원천까지 추적된다.
5. 비로그인 계획이 로그인 후 같은 계정으로 안전하게 귀속된다.
6. 브라우저 E2E가 목 데이터 없이 주요 흐름을 통과한다.
7. 가격 알림 API·화면·worker를 호출하거나 완료 조건으로 검사하지 않는다.

